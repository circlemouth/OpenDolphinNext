# Codex Automation Prompts Revised

## 目的
この文書は、`docs/server-modernization/planning/codex_automation_workplan_revised.md` の各タスクを実行するときに参照する補助指示集である。  
進捗判定の第一正本は `codex_automation_workplan_revised.md` であり、この文書はそれを補助する。

## 使い方
- まず `codex_automation_workplan_revised.md` で未完了先頭タスクを特定する
- ここでは、そのタスク ID に対応する節だけを参照する
- 指示が広すぎる場合は、2 時間以内に終わる範囲へ自分でさらに狭めてよい
- blocker 条件に当たる場合は、その時点で停止する
- 1 タスク完了後に 20 分以上の余力があり、次タスクの必要ファイルと確認方法をすぐ特定できるなら、次タスクへ進むことを原則とする
- 次タスクへ進まない場合は、「時間不足」「blocker」「影響範囲未確定」などの停止理由を実行結果へ明記する

## 共通ルール
- `target/` は編集しない
- `tools/flyway/sql` を migration 正本とする
- `src/main/resources/db/migration` は必要に応じて同期する
- source 不足、仕様未確定、変更範囲外テスト失敗時は停止する
- 後方互換性は考慮しない
- ただし、現時点で切ると仕様判断が必要な契約は、根拠なしに削らない

---

## D-01 / D-02 文書整備用プロンプト
### 目的
automation が `server_modernization_wbs_detailed.md` の全チェック状態に引きずられず、
`codex_automation_workplan_revised.md` を第一正本として参照するようにそろえる。

### 必須作業
- master prompt
- workplan revised
- prompts revised  
の 3 文書で、進捗判定の優先順位を一致させる

### 完了条件
- 3 文書すべてで、第一正本が `codex_automation_workplan_revised.md` になっている
- `server_modernization_wbs_detailed.md` は参考資料扱いになっている
- blocker 条件がそろっている

---

## P1-01 ホットパス改善対象の最小確認テスト追加
### 目的
後続タスクで触る経路に対して、変更前後の最低限の確認ポイントを先に置く。

### 作業対象候補
- ORCA transport
- chart event history
- PVT 初期化 / 追加
- `/docinfo/all`
- 全患者取得 API

### やること
- 既存テストを調べる
- 近い粒度の契約テストまたは単体テストを最小限追加する
- 1 実行で広げすぎない。最優先の 1〜2 経路だけでもよい

### やってはいけないこと
- 今回の変更範囲を超えた包括テスト整備
- 大規模なテスト基盤刷新

### 完了条件
- 次段タスクで壊れたときに最小限検知できる

---

## P1-02 migration 正本ルール整備
### 目的
DB 変更時の迷いをなくす。

### やること
- `tools/flyway/sql` が正本であることを文書へ明記する
- `src/main/resources/db/migration` をどう同期するかを一箇所にまとめる

### 完了条件
- DB 変更タスクの参照先が明確になっている

---

## P2-01 ORCA transport の設定キャッシュと client 寿命分離
### 目的
設定 refresh のたびに `HttpClient` を作り直す構造をやめる。

### 主な対象ファイル候補
- `server-modernized/src/main/java/open/dolphin/orca/transport/RestOrcaTransport.java`
- `server-modernized/src/main/java/open/dolphin/orca/transport/OrcaHttpClient.java`

### やること
- どこで設定を読み直しているか確認する
- どこで `HttpClient` / `OrcaHttpClient` を作っているか確認する
- 同一設定なら再利用、変更時のみ差し替えの方向へ整理する
- 設定の寿命と送信 client の寿命を分離する

### 完了条件
- 設定 refresh ごとに必ず client 再作成、という構造が消える

### blocker
- 設定値の同一性判定が、コードと既存資料だけでは定義できない

---

## P2-02 設定 fingerprint による差し替え判定
### 目的
本当に設定が変わったときだけ transport を差し替える。

### やること
- transport に効く設定項目を特定する
- fingerprint または等価比較を入れる
- 無変更なら既存 instance 再利用
- 変更時のみ差し替え

### 完了条件
- 同一設定 refresh で再生成しない

---

## P2-03 ORCA transport テスト追加
### 目的
P2-01, P2-02 の変更を確認する。

### やること
- 同一設定で再利用されるテスト
- 設定差分で差し替わるテスト
- 例外的な refresh でも壊れない最小確認

### 完了条件
- 変更意図がテストに残っている

---

## P3-01 SSE 配信経路から purge を外す
### 目的
配信ホットパスで delete / purge をしない。

### 主な対象ファイル候補
- `ChartEventSseSupport`
- `ChartEventHistoryRepositoryImpl`
- 関連 service

### やること
- 配信時処理から purge 呼び出しを外す
- 保存処理と purge 処理の責務を分離する

### 完了条件
- 配信経路に purge が直結していない

---

## P3-02 purge を定期経路へ移す
### 目的
履歴掃除を別経路へ分ける。

### やること
- scheduler または maintenance service を使って purge を動かす
- 配信失敗と purge 失敗を独立させる
- purge の頻度や上限は既存文脈で合理的に決める。決められなければ停止する

### 完了条件
- purge 実行が配信のレイテンシを直接増やさない

---

## P3-03 chart event history テスト追加
### 目的
保存と掃除の分離を確認する。

### やること
- 保存される
- 配信時には purge しない
- purge は別経路で動く
を最小確認する

### 完了条件
- 分離意図がテストでわかる

---

## P4-01 initializePvtList 棚卸し
### 目的
どこで DB 往復が増えているかを明確にする。

### やること
- `initializePvtList`
- `addPvt`
周辺の問い合わせを読み、患者ごとの追加参照箇所を特定する

### 成果物
- 変更対象メソッド
- 減らせる問い合わせ
- 次タスクで触るファイル候補
を workplan に短く残す

### 完了条件
- P4-02 に入る対象が明確である

---

## P4-02 initializePvtList の一括取得化
### 目的
一覧初期化時の患者ごとの追加問い合わせを減らす。

### やること
- join または一括取得に寄せられる箇所を優先して整理する
- Java 側で集計しているだけのものは、無理のない範囲で query 側へ寄せる
- 1 回で全部やろうとしない。最も効く 1 経路から着手してよい

### 完了条件
- 患者ごとの再問い合わせが一部でも減っている

---

## P4-03 addPvt の idempotent 化
### 目的
メモリ走査頼みの重複防止から離れる。

### やること
- 業務キー候補を確認する
- DB 一意制約または既存キー前提で idempotent に寄せる
- メモリ全走査を主軸にしない
- 既存順序の副作用が大きい場合は、根拠がない限り広げない

### blocker
- 業務キーがコードと文書から合理的に定まらない

### 完了条件
- 重複防止の中心がメモリ全走査ではなくなる

---

## P4-04 addPvt テスト追加
### 目的
受付追加の安全確認を置く。

### やること
- 通常追加
- 重複追加
- 業務キー重複時の扱い
を最小限確認する

### 完了条件
- addPvt の変更が壊れたときに検知できる

---

## P5-01 `/docinfo/all` 契約棚卸し
### 目的
全件一括返却 API を安全にページングへ寄せる準備をする。

### やること
- レスポンス構造を読む
- 一覧に必要な項目と、詳細取得でよい項目を分ける
- 既存テストで守られている点を確認する

### 完了条件
- P5-02 で何を変えるかが明確

---

## P5-02 `/docinfo/all` ページング化
### 目的
重い一覧 API を一括返却前提から外す。

### やること
- `page/size` か `offset/limit` を導入する
- 一覧では本文や添付実体を無理に抱えない
- 既存クライアント契約を切る判断が必要なら、根拠が文書にあるか確認する

### blocker
- 既存クライアント契約の扱いがコードと文書から確定できない

### 完了条件
- 一覧 API がページング前提になっている

---

## P5-03 全患者取得 API のページング化
### 目的
ページングなし全件取得を主経路から外す。

### やること
- resource と service を確認する
- 画面用途に必要な件数だけ返す形へ寄せる
- デフォルト制限を入れる

### 完了条件
- 主な全患者取得が無制限でなくなる

---

## P6-01 AttachmentStorageManager の stream 化
### 目的
添付のホット経路で `byte[]` 全載せを減らす。

### やること
- 読み込み・書き込みの hot path を確認する
- `byte[]` が必須でない経路から stream 化する
- public 契約を壊さない範囲で内部を改善する

### 完了条件
- 少なくとも 1 本の主要経路で全載せが減る

---

## P6-02 患者画像レスポンスの stream 化
### 目的
画像返却時の不要な全載せを減らす。

### やること
- `PatientImagesResource` の返却経路を確認する
- ストリーム返却へ寄せられる部分を整理する

### 完了条件
- 主要画像返却経路がより軽い構造になっている

---

## P6-03 外部マスタ取得の stream 化
### 目的
大きな外部データ取得時のメモリ全載せを減らす。

### やること
- `MasterUpdateService` 周辺を確認する
- ダウンロード、展開、取り込みのうち stream 化可能な部分を改善する

### 完了条件
- 一括メモリ保持が減る

---

## P7-01 患者同期の既存患者先読み
### 目的
1 人ずつ存在確認する構造を減らす。

### やること
- 取得バッチ単位で既存患者を先にまとめて読む
- 存在判定をメモリ側の map で処理できるように寄せる

### 完了条件
- 件数比例の存在確認往復が減る

---

## P7-02 患者同期 upsert の一括化
### 目的
`facility_id + patient_id` を基準に、まとめ処理へ寄せる。

### やること
- 業務キーを確認する
- flush 粒度と update/insert 分岐を見直す
- 可能ならバッチ upsert に寄せる

### blocker
- 業務キーの確定に必要な根拠がない

### 完了条件
- 同期の DB 往復がさらに減る

---

## P8-01 ORCA マスタ検索条件式整理
### 目的
重い `LIKE` と `CAST/UPPER` の使用を減らす。

### やること
- 最も負荷が大きそうな検索条件を優先して整理する
- 前方一致、用途別 API、正規化列のうち、安全に触れるものを選ぶ
- 1 回で全部直そうとしない

### 完了条件
- 少なくとも 1 つの代表検索が軽い構造になっている

---

## P8-02 count の削減
### 目的
不要な `count(*)` を毎回取らない。

### やること
- 本当に count が必要な画面だけを残す
- それ以外は省略または遅延化する

### 完了条件
- 不要な count が主要経路から減る

---

## P8-03 患者検索 API 分離
### 目的
多段分岐検索を用途別 API へ寄せる。

### やること
- 名前、かな、数字検索を整理する
- 1 リクエスト 1 目的に近づける

### 完了条件
- 検索の見通しが改善する

---

## P9-01 SSE 優先化
### 目的
通知基盤の主経路を SSE に寄せる。

### やること
- 旧 AsyncContext 経路を読んで、どこまで現用か確認する
- 新規利用を増やさない形で凍結する
- 必要なら文書に明記する

### 完了条件
- 通知基盤の主経路が曖昧でない

---

## P9-02 facility context cleanup
### 目的
施設ごとの context が増え続ける構造を緩める。

### やること
- context の保持箇所を確認する
- cleanup 条件を入れる
- gauge の strong reference 保持を見直す

### 完了条件
- facility context の寿命管理が入っている

---

## P9-03 共有リスト構造見直し
### 目的
書き込みの多い箇所で `CopyOnWriteArrayList` を避ける。

### やること
- 書き込み頻度が高い箇所を特定する
- 代替構造へ寄せる
- スレッド安全性を落とさない

### 完了条件
- 少なくとも 1 つの主要箇所で構造見直しが済む

---

## P10-01 `addPvt()` 分割
### 目的
巨大メソッドを責務単位に分ける。

### やること
- `addPvt()` を前処理、重複確認、登録、通知などへ分ける
- 振る舞い変更は最小にとどめる

### 完了条件
- メソッドが短くなり、テストしやすい

---

## P10-02 `RestOrcaTransport` 責務分離
### 目的
設定解決、client 管理、送信責務を分ける。

### やること
- P2 で入れた変更を読みやすい構造へ整える
- helper / manager 抽出を検討する

### 完了条件
- transport の責務境界が明確

---

## P10-03 `KarteServiceBean` 一覧責務分離
### 目的
一覧 API と重い詳細構築の境界をはっきりさせる。

### やること
- `/docinfo/all` に関係する部分を抽出する
- 一覧と詳細の責務を分ける

### 完了条件
- 一覧 API 改修がしやすい構造になる

---

## P11-01 旧設定読み込み棚卸し
### 目的
基盤刷新前に、古い設定経路を一覧化する。

### やること
- `custom.properties`
- JBoss / WildFly 固有パス
- ローカルファイル保存状態
を列挙する

### 完了条件
- 切り落とし候補一覧ができる

---

## P11-02 設定優先順位整理
### 目的
DB、環境変数、ファイル設定の優先順位を明確にする。

### やること
- ORCA
- attachment storage
- license / runtime state
などを対象に、今後の優先順位を整理する

### 完了条件
- 次段の設定刷新が始めやすい

---

## P12-01 ビルド成果物を運用対象から外す
### 目的
レビューと CI の無駄を減らす。

### やること
- `target/`
- `__MACOSX`
- zip 展開ゴミ
を対象外にする
- ignore と説明文書を整える

### 完了条件
- 生成物が常時レビュー対象に混ざらない

---

## P12-02 次段計画更新
### 目的
今回の改善群の次に進む大きい刷新テーマを整理する。

### やること
- ここまでの結果を踏まえて workplan を更新する
- 参考資料が必要なら WBS も補足する

### 完了条件
- 人間レビュー後に次段へ進める

---

## 実行結果の書き方
各実行の最後に、少なくとも次を残すこと。

- 着手タスク ID
- 実施内容の要約
- 主な変更ファイル
- 実施テスト
- blocker の有無
- 次回先頭タスク ID
