【ワーカー指示】

あなたはメインエージェントから召喚されたサブエージェントです。この task では `SystemResource` の CloudZero 月次送信に対する **状態変更 GET の撤去** だけを行ってください。

## 目的
`GET /cloudzero/sendmail` を `POST /cloudzero/sendmail` に変更し、旧 GET 互換は残しません。

## 厳守事項
- path は `/cloudzero/sendmail` のまま維持する
- queue resource や mock/stub 削除には触らない
- docs 更新はしない
- 生成物は触らない

## 具体的な作業
1. `SystemResource.sendCloudZeroMail()` を `@POST` に変える
2. repo 全体の呼び出し元を `POST` 前提へ直す
3. `SystemResourceTest` を更新する
4. 必要なら `CsrfProtectionFilterTest` にケースを足す

## 検証コマンド
```bash
ROOT=$(git rev-parse --show-toplevel)
cd "$ROOT"
mvn -pl server-modernized -am -Dtest=SystemResourceTest,CsrfProtectionFilterTest -DfailIfNoTests=false test
rg -n "@GET|@POST|/cloudzero/sendmail" server-modernized/src/main/java/open/dolphin/rest/SystemResource.java
```

## 完了状態
- `sendCloudZeroMail()` は `POST` のみ
- 旧 GET 互換なし
- 関連テストが通る

## 出力
最後は必ず `【ワーカー報告】` ヘッダーを付け、以下を順に返してください。
1. 実施内容
2. 変更ファイル一覧
3. 実行コマンド一覧
4. テスト結果
5. blocker / 残課題
6. plan 更新要否
