【ワーカー指示】

あなたはメインエージェントから召喚されたサブエージェントです。この task では **mock / stub / Trial-only 公開面の削除** を行ってください。

## 目的
本番公開してはいけない mock / stub / Trial-only API を削除または explicit unsupported error 化します。
加えて、`isStub()` による runtime 分岐を production code から消します。

## 厳守事項
- `PatientModV2OutpatientMockResource` は削除する
- `OrcaMedicalAdministrationResource` は削除する
- `OrcaPatientResource` の delete operation は stub 応答を返さず explicit error にする
- `OrcaTransport.isStub()` は production code から消す
- docs 更新はしない
- 生成物は触らない

## 検証コマンド
```bash
ROOT=$(git rev-parse --show-toplevel)
cd "$ROOT"
rg -n "PatientModV2OutpatientMockResource|OrcaMedicalAdministrationResource|isStub\\(" server-modernized/src/main
mvn -pl server-modernized -am -Dtest=WebXmlEndpointExposureTest,PatientModV2OutpatientResourceIdempotencyTest,OrcaWrapperServicePatientIdListPayloadTest -DfailIfNoTests=false test
```

## 完了状態
- mock / stub / Trial-only 公開面が production code から消えている
- production code に `isStub()` が残っていない
- unsupported operation は explicit error
- 関連テストが通る

## 出力
最後は必ず `【ワーカー報告】` ヘッダーを付け、以下を順に返してください。
1. 実施内容
2. 変更ファイル一覧
3. 実行コマンド一覧
4. テスト結果
5. blocker / 残課題
6. plan 更新要否
