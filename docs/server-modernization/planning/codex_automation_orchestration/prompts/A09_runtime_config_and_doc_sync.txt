【ワーカー指示】

あなたはメインエージェントから召喚されたサブエージェントです。この task では **typed config 正本化と文書同期** を行ってください。

## 目的
typed config を正本に揃え、`custom.properties` fallback 記述や実装との食い違いを消します。
この task では優先箇所だけを確実に直します。

## 厳守事項
- `custom.properties` fallback を文書から消す
- `ChartEventHistoryPurgeScheduler` の default は OFF にする
- `RuntimeConfigurationSupport` を helper の正本にする
- 指定対象以外の config 直読み一掃へ広げない
- 生成物は触らない

## 対象
- `server-modernized/README.md`
- `server-modernized/config/server-modernized.env.sample`
- `RuntimeConfigurationSupport.java`
- `OperationsHealthResource.java`
- `ChartEventHistoryPurgeScheduler.java`
- `PatientImagesResource.java`
- `OrcaPatientSyncScheduler.java`

## 検証コマンド
```bash
ROOT=$(git rev-parse --show-toplevel)
cd "$ROOT"
mvn -pl server-modernized -am -Dtest=RuntimeConfigurationSupportTest,ServerConfigurationValidatorTest,PatientImagesResourceTest -DfailIfNoTests=false test
rg -n "custom.properties" server-modernized/README.md server-modernized/config/server-modernized.env.sample
```

## 完了状態
- `README.md` と env sample と実装 default が一致している
- 優先対象ファイルの config 解決が helper 経由
- scheduler default の説明と実装が一致している

## 出力
最後は必ず `【ワーカー報告】` ヘッダーを付け、以下を順に返してください。
1. 実施内容
2. 変更ファイル一覧
3. 実行コマンド一覧
4. テスト結果
5. blocker / 残課題
6. plan 更新要否
