# 詳細工程表（server-modernization 当面作業）

- 更新日: 2026-03-13
- RUN_ID: 20260313T054324Z
- 位置付け: `server-modernized` の当面作業を順番に進めるための参考資料。進捗判定と完了判定の第一正本ではない。
- task ID の扱い: `docs/server-modernization/planning/codex_automation_workplan_revised.md` の task ID とは同一視しない。番号が一致しても意味・完了条件が異なる場合がある。
- 完了判定: `docs/server-modernization/planning/codex_automation_workplan_revised.md` を優先し、本書のチェック状態だけで完了判定しない。
- 運用: 記載タスクは原則として上から順に消化する。`docs/DEVELOPMENT_STATUS.md`、`AGENTS.md`、最新のユーザー/マネージャー指示、`codex_automation_workplan_revised.md` と矛盾する場合は、それらを優先する。
- 参照入口: `docs/server-modernization/README.md`

A列は ☐ / ☑ で更新します。優先 S は今すぐ着手、A は続けて着手、B は前工程の完了後に進めます。

| 完了 | WBS ID | フェーズ | 優先 | 担当ロール | 想定工数 | 作業項目 | 対象モジュール・ファイル | 依存 | 具体作業 | 完了条件 | 成果物 | 直近の一手 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ☑ | P0-01 | 0. 現状把握 | S | アーキテクト | 0.5日 | ソース受領・解凍・モジュール確認 | common.zip / server-modernized.zip / pom.xml / src 配下全体 | - | 2つのアーカイブを展開し、モジュール構成、ビルド方式、主要パッケージ、テスト配置、設定ファイルの置き場所を確認する。対象リポジトリに build 生成物が含まれているため、src と target を分けて見ることも明記する。 | 担当者全員が、どこに何があるかを迷わず説明できる。対象モジュールの一覧と役割が1枚にまとまっている。 | 構成一覧、ディレクトリマップ、初回確認メモ | 構成一覧をリポジトリ直下の docs へ置き、以後の作業で参照する。 |
| ☑ | P0-02 | 0. 現状把握 | S | アーキテクト | 0.5日 | 主要パッケージと巨大クラスの棚卸し | open/dolphin/rest/** / open/dolphin/session/** / open/orca/rest/** / open/dolphin/touch/** / common/src/main/java/open/dolphin/infomodel/** | P0-01 | クラス数、1,000行超のクラス、責務が集中している箇所を洗い出す。特に KarteServiceBean、KarteResource、OrcaMasterResource、OrcaResource、DolphinResourceASP、PVTServiceBean を熱点として明示する。 | 巨大クラス一覧と、先に分割すべき理由が説明できる。主要改修対象に優先順位が付いている。 | ホットスポット一覧、優先順位表 | ホットスポット一覧をこの工程表とひも付け、各フェーズの対象に反映する。 |
| ☑ | P0-03 | 0. 現状把握 | S | アーキテクト | 0.5日 | 旧互換レイヤーの洗い出し | open/dolphin/touch/** / open/dolphin/shared/legacytouch/** / common/src/main/java/open/dolphin/converter/** / web.xml / common/pom.xml | P0-01 | 旧 Touch、ASP、XML、plist 風レスポンス、converter 群、legacy-wildfly10、独自 jakarta.naming ブリッジなど、後方互換のために残っている層を一覧化する。削除候補を曖昧にせず、ファイル単位で示す。 | 削除対象の候補が package と file の両方で定義され、関係者間で解釈がぶれない。 | 互換レイヤー削除候補一覧 | 削除対象候補を P2 フェーズの具体タスクへ分解する。 |
| ☑ | P0-04 | 0. 現状把握 | S | アーキテクト | 0.5日 | 高リスク領域の抽出 | open/dolphin/orca/** / open/orca/rest/** / META-INF/persistence.xml / open/dolphin/storage/attachment/** / AdminConfigStore などの各 Store | P0-01 | 停止時の影響が大きい領域を ORCA、永続化、添付保存、ローカル設定保存、PVT 受信に分けて整理する。どこを先に固めないと後続が動けないかを明文化する。 | 高リスク領域ごとの影響範囲と、先に守るべき業務が説明できる。 | リスク一覧、優先着手順 | P1 で守る業務を固定し、P5・P6・P7・P8 の依存順を確定する。 |
| ☑ | P0-05 | 0. 現状把握 | S | アーキテクト | 1日 | 刷新方針を1枚に固定する | docs/modernization/architecture-principles.md（新規） | P0-02, P0-03, P0-04 | 後方互換性を考慮しないこと、旧入口を削除すること、ORCA を別境界に出すこと、common を解体すること、ローカルファイル保存をやめることを、文章で固定する。議論の前提を揺らさないための文書にする。 | 関係者が同じ前提で判断でき、仕様確認のたびに方針を聞き直さなくてよい状態になる。 | 刷新方針書、採否基準、非採用案の記録 | 方針書の見出しを先に作り、当日中に承認欄付きで配布する。 |
| ☑ | P0-06 | 0. 現状把握 | A | アーキテクト | 0.5日 | 一時据え置き領域を明記する | reporting/** / open/dolphin/reporting 相当機能 / 影響の小さい共通部品 | P0-05 | 今回の刷新で直ちに置き換えないものを決める。たとえば帳票出力は、コアの刷新を止めない限り一時据え置きにする、などの扱いを決める。 | 対象外と後続対象が分かれ、担当者が余計な横道に入らない。 | 対象外一覧、後続候補一覧 | 対象外一覧を工程表に反映し、誤って着手しないよう周知する。 |
| ☑ | P1-01 | 1. 守る業務を固定 | S | 業務担当 + QA | 1日 | 最重要業務フローを決める | 患者登録 / 受付 / カルテ保存・参照・改訂 / ORCA 連携 / 添付画像 / 管理設定 | P0-05 | 何を壊してはいけないかを業務単位で決める。画面や古い API 形ではなく、患者登録できる、カルテを保存して読める、会計連携できる、という業務結果で定義する。 | 最重要フローが 5〜8 本に絞られ、全員が同じ名称で呼べる。 | 業務フロー一覧、優先度付き判定表 | まず受付、カルテ、ORCA、添付の 4 本を第1群として確定する。 |
| ☑ | P1-02 | 1. 守る業務を固定 | S | QA | 1日 | 各業務フローの受け入れ条件を書く | docs/modernization/acceptance-criteria.md（新規） | P1-01 | 患者登録なら何が保存されれば合格か、カルテ改訂なら旧版と新版の関係がどう残れば合格か、ORCA 連携なら何が返れば合格かを文で書く。誰が見ても合否判断が同じになる文章にする。 | 各フローごとに入力、期待結果、確認項目、失敗時の扱いがそろっている。 | 受け入れ条件一覧 | テンプレートを1つ作り、残りの業務フローへ横展開する。 |
| ☑ | P1-03 | 1. 守る業務を固定 | S | DB担当 + QA | 2日 | 基準データとテスト用 fixture を作る | src/test/resources/** / tools/flyway/** / db/migration/** | P1-01 | 患者、保険、カルテ、画像、ORCA 連携結果の最小セットを作る。新旧比較がしやすいように、テストデータは少数で意味が明確なものにそろえる。 | 担当者が同じデータセットで再現でき、手元差が出ない。 | 基準データ SQL、fixture JSON/XML、初期化手順書 | 最小患者 3 件、カルテ 3 パターン、画像 2 パターンから始める。 |
| ☑ | P1-04 | 1. 守る業務を固定 | S | QA | 2日 | 患者登録・更新の性格確認テストを書く | PatientServiceBean / PatientModV2OutpatientResource / 関連 REST テスト | P1-02, P1-03 | 患者新規登録、更新、重複扱い、主保険の扱い、患者 ID の決まり方を固定する。古い API の形ではなく、登録結果の業務整合性を検証する。 | 患者登録の成功条件と失敗条件が自動テストで再現できる。 | 患者系テスト群、テストケース一覧 | 既存テストを再利用できるものと書き直すものを分ける。 |
| ☑ | P1-05 | 1. 守る業務を固定 | S | QA | 3日 | カルテ保存・参照・改訂の性格確認テストを書く | KarteServiceBean / KarteResource / KarteRevisionServiceBean / 関連 DTO | P1-02, P1-03 | 文書作成、取得、改訂、画像参照、署名や整合性の確認をテストに落とす。XML の戻り形ではなく、患者にひも付いた文書の結果が正しいかで判定する。 | カルテ操作の主要業務が自動テストで通り、改訂時の版管理ルールが固定される。 | カルテ系テスト群、業務ルール一覧 | まず単純な単票保存と改訂1回のケースから固める。 |
| ☑ | P1-06 | 1. 守る業務を固定 | S | ORCA担当 + QA | 3日 | ORCA 連携の性格確認テストを書く | open/dolphin/orca/** / open/orca/rest/** / src/main/resources/orca/stub/** | P1-02, P1-03 | 患者検索、患者更新、受付、オーダ、会計関連など、今回残す ORCA 機能を決めてテストにする。既存 stub を使い、少なくとも正常系と代表的な失敗系を用意する。 | ORCA 側の仕様差分を見逃さず、刷新後も同じ業務結果を返せる。 | ORCA 連携テスト群、機能別判定表 | 機能を一覧化し、今回は残すものに印を付ける。 |
| ☑ | P1-07 | 1. 守る業務を固定 | A | QA | 2日 | PVT 受信と JMS 連携の性格確認テストを書く | PvtService / MessageSender / PVTServiceBean / open/dolphin/msg/** | P1-02, P1-03 | PVT を受信したら何が登録され、重複時にどう扱い、失敗時にどの情報が残るかを固定する。ソケット受信そのものではなく、受信後の業務結果に注目する。 | PVT 入力から業務データ反映までの合否が自動で判定できる。 | PVT 系テスト群、入力サンプル | 代表メッセージを3種類選び、再生できる形へ整理する。 |
| ☑ | P1-08 | 1. 守る業務を固定 | A | QA | 1.5日 | 添付画像・PDF の保存と取得の性格確認テストを書く | AttachmentStorageManager / PatientImagesResource / 関連 service | P1-02, P1-03 | 画像や PDF が患者と正しくひも付き、取得時に内容が変わらないことをテストで固定する。大きいファイルは別途負荷試験で見るので、ここでは代表サイズでよい。 | 添付保存と取得の結果が比較できる。 | 添付系テスト群、比較手順書 | 画像 1 枚、PDF 1 件の最小ケースから作る。 |
| ☑ | P1-09 | 1. 守る業務を固定 | A | QA + セキュリティ担当 | 1.5日 | 管理設定と認証まわりの現行挙動を固定する | AdminAccessResource / AdminOrcaConnectionResource / SessionAuthResource / security/** | P1-02, P1-03 | 設定保存、認証、権限不足時の応答、ログアウトなど、管理系の最小挙動を固定する。今後方式を変えても、必要な業務操作だけは守れるようにする。 | 管理者操作の合否が自動テストで判定できる。 | 管理系テスト群、権限一覧 | 管理者、一般ユーザー、無権限の3パターンを先に準備する。 |
| ☑ | P1-10 | 1. 守る業務を固定 | S | 基盤担当 | 1日 | 性格確認テストを CI で常時回す | pom.xml / CI 設定（新規） / surefire / failsafe | P1-04, P1-05, P1-06 | 刷新作業のたびに壊れたことがすぐ分かるよう、基準テストを CI へ組み込む。PR ごとに最低限回るものと、夜間に回す重いものを分ける。 | PR 単位で合否が見え、主経路の退行を早期に検知できる。 | CI ジョブ、実行手順、失敗時の対応手順 | まず患者・カルテ・ORCA の軽量版を PR 用に登録する。 |
| ☑ | P2-01 | 2. 旧入口を閉じる | S | API担当 | 1日 | 現行の公開入口を一覧化する | web.xml / open/dolphin/rest/** / open/dolphin/touch/** / open/orca/rest/** | P0-05, P1-01 | HTTP 入口、サーブレット、REST パス、Touch/ASP 入口、ORCA 入口を全部並べる。誰が何を呼ぶのか分からない入口は、その時点で要確認にする。 | 公開入口一覧と用途が1枚で見える。 | エンドポイント台帳、利用者メモ | web.xml の記述と Resource クラスのパスを照合して埋める。 |
| ☑ | P2-02 | 2. 旧入口を閉じる | S | API担当 | 1日 | 削除マトリクスを作る | P2-01 の台帳 / docs/modernization/remove-matrix.md（新規） | P2-01, P1-01 | 入口ごとに、削除、置換、統合、後回しを決める。後方互換は取らないため、Touch、ASP、XML 専用口は基本的に削除扱いにする。 | 入口ごとの扱いが確定し、削除時に迷いが出ない。 | 削除マトリクス、承認記録 | Touch 系と XML 系を先に赤く塗り、残す入口だけを白にする。 |
| ☑ | P2-03 | 2. 旧入口を閉じる | S | API担当 | 1日 | 新 API の名前空間と単位を決める | 新設 API 設計書 / /api/v1 配下の resource 設計 | P2-02 | 新しい入口を /api/v1 にそろえ、患者、カルテ、受付、ORCA 連携、添付、管理の単位に分ける。レスポンス形式は JSON に統一し、XML 専用口は残さない。 | 新 API の一覧と URI 命名規則が決まり、各担当が並行で実装できる。 | API 設計書、パス命名規則 | 患者、カルテ、ORCA の3系統だけ先に定義する。 |
| ☑ | P2-04 | 2. 旧入口を閉じる | S | API担当 | 2日 | Touch パッケージと ASP 入口を削除する | open/dolphin/touch/** / DolphinResourceASP.java / touch 関連テスト | P2-03, P1-10 | Touch 専用 DTO、service、resource、ASP 入口をリポジトリから削除する。削除前に、必要な業務機能が新 API 側へ移る計画になっていることを確認する。 | touch 配下が消え、ビルドが通り、必要な機能が別入口へ移る準備ができている。 | 削除コミット、影響一覧、代替先一覧 | まず touch 配下の依存先検索を行い、残存参照を一覧化する。 |
| ☑ | P2-05 | 2. 旧入口を閉じる | A | API担当 | 1日 | LegacyTouch 抽象層を削除する | open/dolphin/shared/legacytouch/LegacyTouchAbstractResource.java / 参照先全体 | P2-04 | Touch の土台になっている抽象層と補助クラスを削除する。継承関係が残っていると後で誤って再利用されるため、早い段階で消す。 | LegacyTouch 依存がコード上から消える。 | 削除コミット、参照ゼロ確認 | 継承元・利用元を IDE 検索で洗い出し、まとめて切る。 |
| ☑ | P2-06 | 2. 旧入口を閉じる | S | API担当 | 2日 | XML 専用エンドポイントを削除する | KarteResource.java ほか XML 応答を返す resource / converter / parser | P2-03, P1-10 | XML や plist 風の返却を前提にしている口を削除し、JSON へ統一する。削除対象は resource だけでなく、古いシリアライズ補助も含めて追う。 | XML 専用入口が残らず、公開契約が JSON にそろう。 | 削除コミット、API 差分一覧 | まず KarteResource で XML を返している箇所を切り出し、削除範囲を確定する。 |
| ☑ | P2-07 | 2. 旧入口を閉じる | S | DB担当 | 2日 | common/converter 群を削除する | common/src/main/java/open/dolphin/converter/** / ConverterModelReferences.java | P2-04, P2-06, P3-04 | 旧契約に合わせるための converter 群を削除する。必要な変換は新 API DTO への明示的な mapper に置き換える。 | converter 配下が整理され、ダミー参照が消える。 | 削除コミット、新 mapper 一覧 | converter を機能単位で棚卸しし、残す変換だけ新 mapper に移す。 |
| ☑ | P2-08 | 2. 旧入口を閉じる | A | 基盤担当 | 1日 | legacy-wildfly10 と独自 naming ブリッジを削除する | common/pom.xml / server-modernized/pom.xml / src/main/java/jakarta/naming/InitialContext.java | P0-05 | 旧コンテナ互換のために残っているビルドプロファイル、shade 設定、独自 InitialContext ブリッジを削除する。実行基盤の前提を一つに寄せる。 | ビルド設定から旧互換向けの特殊処理が消える。 | pom 差分、削除コミット、ビルド確認結果 | まず profile と shade の参照箇所を洗い出し、削除の順番を決める。 |
| ☑ | P2-09 | 2. 旧入口を閉じる | A | 基盤担当 | 1.5日 | web.xml と各種 descriptor を最小化する | WEB-INF/web.xml / jboss-web.xml / jboss-deployment-structure.xml / META-INF/ejb-jar.xml | P2-03, P2-08 | 使わないサーブレット、フィルタ、古い公開設定を減らす。旧入口を消したのに descriptor 側だけ残ることを防ぐ。 | 公開面が必要最小限になり、不要な設定が残っていない。 | descriptor 差分、公開面一覧 | 削除マトリクスと descriptor を行単位で照合する。 |
| ☑ | P2-10 | 2. 旧入口を閉じる | A | API担当 + QA | 1日 | 新旧 API 差分と移行後契約を文書化する | docs/modernization/api-map.md（新規） / テスト群 | P2-03, P2-04, P2-06 | 残す API、消す API、新しい URI、必須ヘッダ、認証方式、返却形式を1枚にまとめる。担当者が古い Resource を見に行かなくても、新しい契約だけで実装を進められるようにする。 | API 利用者向けの参照文書がそろう。 | API 差分表、実装用一覧 | 患者、カルテ、添付、管理の4系統から先に書く。 |
| ☑ | P3-01 | 3. common を解体する | S | アーキテクト | 1日 | 新しいモジュール境界を決める | common / server-modernized / 新規 module 設計 | P0-05, P2-03 | 少なくとも domain、persistence、api-contract、orca-adapter、ingestion、storage の単位に責務を分ける。どの module がどの module を参照してよいかも同時に決める。 | 依存方向が明文化され、循環参照の再発を防げる。 | モジュール境界図、依存ルール | まず existing package を付箋のように並べ、どこへ移すかを書き出す。 |
| ☑ | P3-02 | 3. common を解体する | S | 基盤担当 | 1.5日 | 新 module のひな型を作る | pom.xml 群 / 新規 module ディレクトリ / テストひな型 | P3-01 | 決めた境界に合わせて Maven module を作り、ビルドとテストの最小構成を用意する。最初から大移動せず、空の module と依存だけ先に確定する。 | ビルドが通る最小 module 群ができる。 | module ひな型、親 pom 差分 | domain と api-contract の2つだけ先に立てて確認する。 |
| ☑ | P3-03 | 3. common を解体する | S | DB担当 | 3日 | JPA entity を common から切り離す | common/src/main/java/open/dolphin/infomodel/** の entity 類 / persistence module | P3-02, P6-01 | 永続化用の entity を shared jar から外し、persistence 専用 module へ寄せる。API や業務ロジックが entity を直接持ち回らないようにする。 | entity の置き場所が一意になり、API 層から直接見えない。 | entity 移動コミット、依存差分 | まず Patient、User、Document、Module の主要 entity から移す。 |
| ☑ | P3-04 | 3. common を解体する | S | API担当 | 3日 | API DTO を entity から分離する | open/dolphin/rest/dto/** / 新 api-contract module / 既存 resource | P3-02, P2-03 | 公開契約として使う DTO を定義し、entity をそのまま JSON に出さないようにする。入力 DTO、出力 DTO、検索条件 DTO を用途別に分ける。 | API 入出力が DTO に統一され、entity 依存が消える。 | DTO 一式、mapper 一式、resource 差分 | 患者とカルテの DTO から先に切り出す。 |
| ☑ | P3-05 | 3. common を解体する | A | API担当 | 2日 | REST 層から entity 直返しをなくす | resource 全体 / mapper 層 / DTO | P3-03, P3-04 | resource で entity をそのまま返している箇所、entity をそのまま受け取っている箇所を DTO 経由へ変える。今後の schema 変更を API 契約から切り離すための作業である。 | resource に entity 露出が残っていない。 | resource 差分、entity 露出ゼロ確認 | 返却型の一覧を作り、entity 型を使っているものから直す。 |
| ☑ | P3-06 | 3. common を解体する | A | アーキテクト | 1日 | audit / util / common の最小責務を決める | common/src/main/java/open/dolphin/audit/** / common/** / util/** | P3-01 | 全モジュールから見える共通部品を最小にする。監査イベント、例外、値オブジェクトなど、本当に共有するものだけを残す。 | 共通部品の範囲が狭まり、曖昧な common が増えない。 | 共通部品一覧、配置ルール | まず util を精査し、何でも入れる箱にしない方針を書く。 |
| ☑ | P3-07 | 3. common を解体する | A | DB担当 | 1日 | ダミー参照と死んでいる補助コードを削除する | ConverterModelReferences.java など参照維持用コード全体 | P2-07, P3-04 | 参照維持のためだけに置いているクラス、未使用のラッパー、古いシリアライズ補助を削除する。残す理由が説明できないものは残さない。 | 未使用コードが減り、参照方向が読みやすくなる。 | 削除コミット、未使用コード一覧 | IDE の参照検索とコンパイラエラーを使い、順に削る。 |
| ☑ | P3-08 | 3. common を解体する | A | 基盤担当 | 1日 | モジュール再編後のビルド構成を整える | 親 pom / 各 module pom / テスト実行順 | P3-02, P3-03, P3-04 | module 追加後のビルド順、テスト順、依存の可視性を調整する。古い module を残したまま移行する期間の扱いもここで決める。 | CI とローカルのどちらでも同じ手順でビルドできる。 | pom 差分、ビルド手順書 | module 間依存を図にして、循環がないことを確認する。 |
| ☑ | P4-01 | 4. アプリケーションコアを分割 | S | API担当 | 3日 | KarteResource を責務ごとに分ける | KarteResource.java / revision 系 resource / image 系 resource / DTO | P3-04, P1-05 | 文書取得、文書保存、改訂、画像参照などを分離し、一つの resource に多数の役割を持たせない。入口ごとに入力検証と認可も整理する。 | KarteResource が小さくなり、機能別に追える。 | 新 resource 群、移行メモ、テスト更新 | まず read 系と write 系に2分割し、その後 revision を独立させる。 |
| ☑ | P4-02 | 4. アプリケーションコアを分割 | S | 業務担当 | 4日 | KarteServiceBean を use case 単位に分ける | KarteServiceBean.java / 関連 session bean / service 層 | P4-01, P6-05 | 保存、取得、改訂、検索、画像処理などを別 service に分ける。トランザクション境界は use case ごとに置き、1 クラスにすべて詰め込まない。 | 主要 use case ごとに service が分かれ、責務が明確になる。 | service 群、呼び出し図、テスト更新 | KarteServiceBean の public method 一覧を作り、似たものからまとめる。 |
| ☑ | P4-03 | 4. アプリケーションコアを分割 | A | API担当 | 3日 | 患者更新・管理系の巨大 resource を分ける | PatientModV2OutpatientResource.java / AdminAccessResource.java / AdminOrcaUserResource.java | P3-04, P1-04, P1-09 | 巨大 resource を機能別に分け、入力検証、権限確認、業務処理、応答生成を分離する。患者更新と管理設定は分岐が多いため、先に責務を割る。 | 1 resource の責務が狭まり、レビューしやすくなる。 | resource 分割コミット、責務一覧 | まず入力 DTO と業務 service を分け、最後に resource を薄くする。 |
| ☑ | P4-04 | 4. アプリケーションコアを分割 | A | セキュリティ担当 | 2日 | 入力検証・認可・監査を横断部品へ出す | security/** / audit/** / filter / interceptor / resource 共通処理 | P3-04 | 各 resource の中に散っている入力検証、権限確認、監査記録を共通化する。業務本体に関係ない処理が use case を読みにくくしない状態へ整える。 | resource から横断処理が減り、監査抜けを防げる。 | 共通 interceptor / helper / 監査仕様 | まず権限確認と監査記録の2つを共通部品に切り出す。 |
| ☑ | P4-05 | 4. アプリケーションコアを分割 | A | API担当 | 1日 | エラー応答形式と request id を統一する | resource 全体 / exception mapper / log filter | P2-03, P4-04 | 例外時の返却形式を JSON に統一し、request id や run id を全系統で付ける。運用時に追跡しやすくし、resource ごとの差をなくす。 | 全 API が同じエラー形式を返し、ログ相関が取れる。 | 共通 error response、運用手順 | まず 400、401、403、404、409、500 の型を定義する。 |
| ☑ | P4-06 | 4. アプリケーションコアを分割 | A | 業務担当 | 2日 | トランザクション境界を見直す | session/** / service 層 / persistence 層 | P4-02, P6-05 | どこからどこまでを一つの更新単位にするかを見直す。外部 I/O をトランザクションの中へ入れない、更新単位を広げすぎない、という原則で整理する。 | 更新単位が明文化され、外部連携待ちで長く握らない。 | トランザクション設計メモ | 患者更新、カルテ保存、添付保存の3系統から確認する。 |
| ☑ | P4-07 | 4. アプリケーションコアを分割 | B | 基盤担当 | 2日 | EJB 固有前提を減らし CDI 中心へ寄せる | session bean / producer / descriptor | P4-02, P2-09 | すべてを一度に変えず、業務 service の新規実装は CDI を基本にする。EJB 固有に縛られている箇所を洗い出し、必要なものだけ残して減らす。 | 新規コードが軽い依存で書ける。 | 設計方針、置換済み service 一覧 | まず新設する service から CDI 化し、既存 service は後追いで切り替える。 |
| ☑ | P4-08 | 4. アプリケーションコアを分割 | A | API担当 + QA | 1.5日 | 実装後の API 文書とテストを更新する | API 設計書 / DTO / テスト群 | P4-01, P4-03, P4-05 | Resource の分割後に文書とテストが古いまま残らないように、同じ変更セットで更新する。仕様差し戻しを減らすため、変更点は差分で書く。 | 文書、実装、テストが一致している。 | 更新済み API 文書、テスト結果 | 変更した resource ごとにレビュー項目を定型化する。 |
| ☑ | P5-01 | 5. ORCA を別境界へ出す | S | ORCA担当 | 1.5日 | ORCA 境界の責務を決める | open/dolphin/orca/** / open/orca/rest/** / 新 orca-adapter module | P0-05, P1-06, P3-01 | ORCA へ渡す入力と受け取る結果を、業務側の言葉で定義する。業務 service からは ORCA の URL、XML 形式、DAO 実装が見えないようにする。 | ORCA adapter の公開 interface が決まり、業務側が直接 XML や HTTP を知らなくてよい。 | 境界設計書、adapter interface | 患者検索、患者更新、受付の3つから interface を定義する。 |
| ☑ | P5-02 | 5. ORCA を別境界へ出す | S | 基盤担当 | 1.5日 | 接続設定と認証情報を外部設定へ移す | OrcaConnectionConfigStore.java / OrcaTransportSettings.java / custom.properties 依存箇所 | P5-01, P8-03 | 施設番号、接続先、資格情報、タイムアウト、リトライ設定をコードやローカルファイルから分離する。読み込み場所と優先順位も固定する。 | 接続設定の読み込み元が一つにそろい、ローカルファイル依存がなくなる。 | 設定仕様書、設定ロード実装 | 現行項目を一覧化し、新設定名へ対応表を作る。 |
| ☑ | P5-03 | 5. ORCA を別境界へ出す | S | ORCA担当 | 2日 | static singleton とローカルキャッシュの前提をなくす | OrcaResource.java / RestOrcaTransport.java / OrcaTransportSettings.java | P5-01 | static 状態、施設単位キャッシュ、アプリ内再読込ロックを整理し、明示的な設定オブジェクトと短寿命の cache へ置き換える。複数ノードでも破綻しない設計にする。 | ノード依存の状態が減り、設定再読込の挙動が読みやすい。 | adapter 実装、設定キャッシュ方針 | まず static フィールド一覧を作り、保持理由を確認する。 |
| ☑ | P5-04 | 5. ORCA を別境界へ出す | S | ORCA担当 | 3日 | HTTP 呼び出しと再試行方針を作り直す | OrcaHttpClient.java / transport 配下 / timeout・retry 設定 | P5-01, P5-02 | 同期ブロック、Thread.sleep、文字列ベースの成否判定をやめ、明示的な timeout、再試行回数、失敗分類、構造化パースへ置き換える。呼び出しログは個人情報を伏せた形へ統一する。 | ORCA 呼び出し失敗時の挙動が再現可能で、設定で調整できる。 | 新 transport 実装、失敗分類表、ログ方針 | 正常系 1 件、タイムアウト 1 件、業務エラー 1 件を先にテスト化する。 |
| ☑ | P5-05 | 5. ORCA を別境界へ出す | A | DB担当 | 3日 | ORCA 専用 DAO を gateway の内側へ閉じ込める | OrcaMasterDao.java / EtensuDao.java / OrcaMasterResource.java | P5-01, P6-05 | ORCA 由来の SQL や検索ロジックを adapter 内部へ閉じ込め、業務 service から raw DAO を見せない。巨大 DAO は検索目的ごとに分ける。 | ORCA 用のデータ取得が業務層へ漏れない。 | gateway 実装、検索別 service | DAO の public method を一覧化し、機能単位で分割案を作る。 |
| ☑ | P5-06 | 5. ORCA を別境界へ出す | A | API担当 | 3日 | ORCA 用 resource を機能別に分割する | OrcaResource.java / OrcaMasterResource.java / open/dolphin/rest/orca/** | P5-01, P5-05 | 巨大 resource を患者、受付、オーダ、病名、レポートなどの単位へ分ける。resource は adapter を呼ぶだけに薄く保つ。 | ORCA 系 resource が用途ごとに分かれ、変更が局所化する。 | 分割済み resource 群、テスト更新 | 利用頻度が高い患者系と受付系から先に分ける。 |
| ☑ | P5-07 | 5. ORCA を別境界へ出す | A | DB担当 | 2日 | 同期状態保存を DB へ移す | OrcaPatientSyncStateStore.java / ローカル JSON 保存箇所 | P5-02, P6-08 | 同期時刻、再開位置、差分取得の状態をローカル JSON ではなく DB へ移す。再デプロイやノード切り替えで状態が飛ばないようにする。 | 同期状態が共有可能になり、再起動で失われない。 | 新 state table、migration、読み書き実装 | 保持したい項目を現行 JSON から抜き出して表にする。 |
| ☑ | P5-08 | 5. ORCA を別境界へ出す | S | QA + ORCA担当 | 2日 | stub を使った adapter 統合試験を整える | src/main/resources/orca/stub/** / Orca*Test.java | P5-04, P5-05, P5-06 | 既存の stub 資産を adapter 用の統合試験へ寄せる。外部本番環境に依存せず、正常系と代表エラーを再現できるようにする。 | adapter 単位で外部連携の退行が検知できる。 | 統合試験群、stub 対応表 | stub の用途と未使用ファイルを先に整理する。 |
| ☑ | P5-09 | 5. ORCA を別境界へ出す | A | 基盤担当 | 1.5日 | ORCA 連携の性能・障害試験を行う | adapter / 接続設定 / メトリクス | P5-08 | 遅延、タイムアウト、多重呼び出し、設定ミス時の挙動を試験する。外部が遅いときに本体のスレッドを不必要に塞がないことを確認する。 | 障害時挙動と復旧手順が説明できる。 | 試験記録、しきい値、運用注意点 | まず 1 件ずつの遅延試験を行い、その後に並列を増やす。 |
| ☑ | P6-01 | 6. データモデルを刷新する | S | アーキテクト + DB担当 | 2日 | entity・domain・API の責務分担を設計する | common infomodel / 新 domain / 新 persistence / api-contract | P3-01, P1-05 | どの情報を DB 都合で持つか、どの情報を業務ルールで持つか、どの情報を公開契約に出すかを分けて定義する。層ごとに禁止事項も書く。 | 三者の役割が分かれ、変更時の波及先が予測できる。 | 責務分担表、層別ルール | Patient、User、Document、Module から先に設計する。 |
| ☑ | P6-02 | 6. データモデルを刷新する | A | DB担当 | 3日 | java.util.Date を java.time へ置き換える | common/src/main/java/open/dolphin/infomodel/** / server 側 entity・DTO 全体 | P6-01 | 日時型を LocalDate、LocalDateTime、OffsetDateTime など用途に応じて分ける。比較、シリアライズ、DB カラム定義も合わせてそろえる。 | 新規コードに Date が残らず、既存主要モデルも移行されている。 | 日時型移行コミット、型対応表 | まず患者生年月日、作成日時、更新日時の3分類を決める。 |
| ☑ | P6-03 | 6. データモデルを刷新する | S | DB担当 | 2日 | ModuleModel の bean_json 保存をやめる設計を決める | ModuleModel.java / ModuleJsonConverter.java / 関連テーブル | P6-01 | Java クラス名に強く依存する bean_json をやめ、型付きテーブルまたは version 付き構造化 JSON へ置き換える方針を決める。検索、更新、監査が可能な形を優先する。 | 置換先の構造が決まり、移行方針が説明できる。 | 置換設計書、移行案比較 | 今ある module 種別を全件棚卸しし、同型のものをまとめる。 |
| ☑ | P6-04 | 6. データモデルを刷新する | S | DB担当 | 4日 | Module 保存形式を実装する | 新テーブル群または versioned JSON 実装 / mapper / service | P6-03 | 決めた保存形式へ実際に移し、保存、取得、改訂、比較ができるようにする。旧形式の読取を残さない前提なので、変換処理は一度の移行で終える。 | Module の主要操作が新形式で通り、旧保存形式に依存しない。 | 新 schema、実装、移行スクリプト | 主要 module 2 種から先に実装し、残りを横展開する。 |
| ☑ | P6-05 | 6. データモデルを刷新する | S | DB担当 | 3日 | 永続化アクセスを repository/query 層へ統一する | session/** / DAO / native query 使用箇所 / persistence module | P3-03, P6-01 | JPA、native SQL、raw JDBC が混ざっている現状を整理し、主 DB への入口を repository と query service にそろえる。用途ごとに read と write を分けてもよい。 | データアクセスの入口が読みやすくなり、調整箇所が分かる。 | repository 一式、query service 一式 | 患者、カルテ、ユーザーの3系統から共通形を作る。 |
| ☑ | P6-06 | 6. データモデルを刷新する | A | DB担当 | 3日 | native query と raw JDBC を棚卸しして書き換える | createNativeQuery 利用箇所 / OrcaMasterDao / EtensuDao など | P6-05 | 使い続ける SQL と置き換える SQL を決める。複雑で SQL が妥当なものは専用 query service へ寄せ、安易な直書きは減らす。 | 各 SQL の置き場所と存在理由が説明できる。 | SQL 棚卸し表、書換え済み実装 | まず createNativeQuery の一覧表を作り、用途で分類する。 |
| ☑ | P6-07 | 6. データモデルを刷新する | A | 基盤担当 | 1.5日 | persistence.xml の手書き class list を整理する | META-INF/persistence.xml / entity 配置 | P3-03, P6-05 | entity の置き場所が決まった後で、手書き class list を整理する。登録漏れや削除漏れで起きる事故を防ぐため、最小構成へ寄せる。 | persistence 設定が今の構成だけを指す。 | persistence 設定差分、起動確認結果 | 不要になった entity 名を先に洗い出す。 |
| ☑ | P6-08 | 6. データモデルを刷新する | S | DB担当 | 3日 | 新 schema の Flyway migration を作る | tools/flyway/** / db/migration/** | P6-03, P6-04, P5-07 | 削除する列、追加する列、新テーブル、index を migration へ落とし込む。新規環境構築と既存環境更新の両方が再現できる形にする。 | 空の DB と既存 DB の両方で migration が通る。 | migration SQL、適用手順、検証記録 | schema 差分を一覧にし、1 変更 1 migration の粒度で分ける。 |
| ☑ | P6-09 | 6. データモデルを刷新する | S | DB担当 + QA | 3日 | 既存データ移行スクリプトを作る | migration 補助 SQL / one-shot 移行ツール（新規） | P6-04, P6-08 | bean_json など旧形式のデータを新形式へ移す一度きりの変換手順を作る。途中失敗時にどこまで進んだか分かる記録も残す。 | 代表データで移行が成功し、件数差異を説明できる。 | 移行スクリプト、件数照合表、再実行方針 | まず検証用 DB のコピーで 1 回通し、差異を記録する。 |
| ☑ | P6-10 | 6. データモデルを刷新する | A | DB担当 | 2日 | index・fetch plan・N+1 を見直す | entity / query service / SQL / 主要画面相当処理 | P6-05, P6-08 | 検索速度、文書取得、画像一覧、ORCA 同期のような重い経路で、取得件数、join、遅い SQL を見直す。必要な index を migration に含める。 | 主要経路の応答時間が基準内に収まる。 | 性能測定記録、index 一覧、改善結果 | カルテ一覧、患者検索、ORCA 同期の3経路から測る。 |
| ☑ | P7-01 | 7. 受信処理を分離する | S | 基盤担当 | 2日 | PvtService の生ソケット受信を別ワーカーへ出す | PvtService.java / 受信起動処理 / worker module（新規） | P1-07, P3-01 | 本体アプリ内で ServerSocket を抱える設計をやめ、専用ワーカーまたは独立プロセスへ出す。受信責務と業務処理責務を分ける。 | 本体から生ソケット受信が外れ、障害切り分けがしやすい。 | worker 実装、接続仕様、起動手順 | 現行の受信入力と業務処理の境界をまずコード上で切り出す。 |
| ☑ | P7-02 | 7. 受信処理を分離する | A | 基盤担当 | 2日 | MessageSender の JMS 消費責務を整理する | MessageSender.java / JMS 設定 / 受信後 service | P1-07, P7-01 | JMS を受けた後に何を同期で行い、何を後続処理へ回すかを整理する。受信側と業務更新側の責務を分ける。 | JMS 受信後の処理段階が明確になる。 | 受信フロー図、分割済み実装 | 現在の onMessage 内の処理を手順ごとに分けて書き出す。 |
| ☑ | P7-03 | 7. 受信処理を分離する | S | 基盤担当 + QA | 2日 | PVT 入力パイプラインの再試行と重複防止を設計する | worker / queue / PVTServiceBean / idempotency 管理（新規） | P7-01, P7-02 | 受信失敗時の再試行、同一メッセージ再送時の重複防止、毒メッセージの退避先を決める。入力が不安定でも本体の状態を壊さないようにする。 | 再試行と重複防止の挙動が設計と試験で確認できる。 | 設計書、試験ケース、実装 | メッセージ識別子として使える項目を現行データから確認する。 |
| ☑ | P7-04 | 7. 受信処理を分離する | A | 基盤担当 | 1日 | ローカルファイル出力依存をなくす | PVTServiceBean.java / export 先固定パス / user.home 利用箇所 | P7-01 | ローカルファイルへ吐く前提の補助処理を削除する。必要な記録は DB、オブジェクトストレージ、集中ログのどれかへ寄せる。 | ノードローカルに依存する出力が残らない。 | 削除コミット、代替先一覧 | 固定パスへ書いている箇所を grep で洗い出す。 |
| ☑ | P7-05 | 7. 受信処理を分離する | A | QA | 1.5日 | 受信メッセージの再生ツールを作る | src/test/resources / worker テスト / replay ツール（新規） | P1-07, P7-03 | 代表メッセージを再生して、受付更新や患者反映を再現できるようにする。本番障害の再現と修正確認を早くするための道具である。 | 入力再生から結果確認までを数分で回せる。 | replay ツール、サンプル入力一式 | まず 1 件の正常メッセージをコマンド 1 つで再生できるようにする。 |
| ☑ | P7-06 | 7. 受信処理を分離する | B | 基盤担当 | 1日 | ワーカーの監視項目とヘルスチェックを作る | worker / metrics / health endpoint | P7-01, P7-03 | 受信停止、再試行詰まり、処理遅延、毒メッセージ件数が見えるようにする。監視無しで運用しない前提を作る。 | 障害兆候が監視で見える。 | 監視項目一覧、health 実装、閾値表 | まず受信件数、失敗件数、最終成功時刻の3つを出す。 |
| ☑ | P8-01 | 8. 添付と設定保存を刷新する | S | 基盤担当 | 2日 | AttachmentStorageManager をストリーミング化する | AttachmentStorageManager.java / upload・download 経路 | P1-08, P6-05 | byte 配列へ丸ごと載せる実装をやめ、入力も出力もストリームで扱う。大きいファイルでヒープを圧迫しないことを優先する。 | 大きい添付でもメモリ使用量が安定する。 | 新 storage 実装、試験結果 | 現在の byte[] 化箇所をすべて洗い出し、置換対象を決める。 |
| ☑ | P8-02 | 8. 添付と設定保存を刷新する | A | 基盤担当 | 1日 | S3 認証を固定資格情報から外す | AttachmentStorageManager.java / config/attachment-storage.sample.yaml | P8-01, P8-03 | コードに近い場所で資格情報を固定しない。環境変数、ロール、シークレット管理など、運用環境に合わせた安全な取得方法へ寄せる。 | 添付機能が安全な資格情報取得で動く。 | 設定仕様、実装差分、運用手順 | 現行設定項目を棚卸しし、どの実行環境で何を使うか決める。 |
| ☑ | P8-03 | 8. 添付と設定保存を刷新する | S | 基盤担当 | 2日 | YAML・properties・JSON の設定読み込みを一本化する | config/*.sample / custom.properties 依存箇所 / 各 Store | P0-05 | 設定値の読み込み先を環境変数、秘密情報、DB 設定の3種に整理し、ローカルファイル読み込みは極力なくす。優先順位と必須項目を固定する。 | 設定読み込みの規則が一つにそろい、担当者が迷わない。 | 設定設計書、設定ローダー、項目一覧 | 現行設定キーを一覧表にして、新しい置き場所を決める。 |
| ☑ | P8-04 | 8. 添付と設定保存を刷新する | S | DB担当 | 3日 | 各種 Store のローカル JSON 永続化を DB 化する | AdminConfigStore.java / MasterUpdateStore.java / OrcaConnectionConfigStore.java / OrcaPatientSyncStateStore.java | P8-03, P6-08 | 管理設定、最終更新時刻、ORCA 接続、同期状態など、ノードローカルへ保存している状態を DB へ移す。履歴が必要なものは別テーブルにする。 | 複数ノードや再デプロイで状態が失われない。 | 新テーブル、repository、migration、移行手順 | Store ごとに保持項目、更新頻度、履歴要否を表にする。 |
| ☑ | P8-05 | 8. 添付と設定保存を刷新する | A | 基盤担当 | 1日 | user.home と build 生成物依存を取り除く | PushEventDeduplicator など user.home 利用箇所 / target 依存運用 | P8-03 | ユーザーのホームディレクトリや target 配下へ状態を置く前提をなくす。本番運用で消えて困るものをすべて外部へ出す。 | ローカルパス依存の状態保存が残らない。 | 削除コミット、代替先一覧 | path 文字列検索で user.home、/opt/jboss、target を拾い出す。 |
| ☑ | P8-06 | 8. 添付と設定保存を刷新する | B | セキュリティ担当 | 1日 | 設定変更の監査と入力検証を加える | 管理 API / 設定保存 service / audit | P8-04 | 設定値を誰が、いつ、何に変えたかを残す。危険な設定値や空欄を保存できないように、管理 API 側でも検証する。 | 設定変更の追跡ができ、危険な誤設定を防げる。 | 監査ログ仕様、入力検証実装 | ORCA 接続設定と添付保存設定から先に監査対象へ入れる。 |
| ☑ | P9-01 | 9. 横断基盤を整える | A | 基盤担当 | 1.5日 | ログ形式を統一する | LogFilter.java / slf4j / jboss logmanager / 各 resource・service | P4-05 | ログ API、出力項目、request id、user id、施設 id などの共通項目を決める。個人情報の出し過ぎも同時に抑える。 | 追跡に必要な項目がそろい、ログ形式がばらつかない。 | ログ方針、共通 logger ルール | まず access log と業務エラーログの2種類を定義する。 |
| ☑ | P9-02 | 9. 横断基盤を整える | A | 基盤担当 | 1.5日 | メトリクス生成を単純化する | MeterRegistryProducer.java / request metrics / ORCA metrics / worker metrics | P7-06, P5-09 | JNDI fallback や registry 掃除のような複雑な回避策を減らし、必要なメトリクスを素直に出す。運用が見るべき値に絞る。 | メトリクス出力経路が読みやすくなる。 | メトリクス一覧、実装差分、監視設定 | まず request 数、error 数、外部連携遅延の3分類へ整理する。 |
| ☑ | P9-03 | 9. 横断基盤を整える | A | セキュリティ担当 | 2日 | 認証・セッション方式を決めて実装する | SessionAuthResource / security/** / web.xml / 新 auth 実装 | P1-09, P2-03, P4-04 | 院内利用に必要な認証を整理し、今後残す方式を一つにそろえる。セッション管理、CSRF 対策、権限表現も合わせて見直す。 | 認証方式と権限制御の方針が固定され、実装が一本化される。 | 認証設計書、権限表、実装、テスト | 現行の権限種別を一覧化し、不要な分岐を減らす。 |
| ☑ | P9-04 | 9. 横断基盤を整える | B | 基盤担当 | 1日 | テスト基盤を JUnit 5 に統一する | common/pom.xml / server-modernized/pom.xml / test 全体 | P1-10, P3-08 | テストフレームワークの世代差をなくし、拡張や並列実行の扱いをそろえる。書き方のばらつきも減らす。 | 新旧 module で同じテスト基盤が動く。 | pom 差分、移行済みテスト一覧 | まず common 側の JUnit 4 テストを一覧化する。 |
| ☑ | P9-05 | 9. 横断基盤を整える | A | 基盤担当 | 1日 | 静的解析を必須ゲートにする | config/static-analysis/** / pom / CI | P3-08 | Checkstyle、PMD、SpotBugs の位置付けを決め、PR 時に必ず見る範囲を固定する。警告を出すだけで終わらせず、基準を決める。 | 最低限の品質基準が自動で守られる。 | CI ゲート設定、除外方針、基準表 | まず新規コードは警告ゼロを原則にする。 |
| ☑ | P9-06 | 9. 横断基盤を整える | B | 基盤担当 | 2日 | 配備方式を簡素化する | war ビルド / WildFly 前提 / descriptor / 実行手順 | P2-09, P4-07 | 今後も WAR を使うか、より軽い実行形へ寄せるかを決める。決めた方式に不要な設定は削る。 | 実行方式が一つにそろい、起動手順が単純になる。 | 配備方針書、起動手順、不要設定削除 | まず現行起動に必要な設定だけを表へ起こす。 |
| ☑ | P9-07 | 9. 横断基盤を整える | A | 基盤担当 | 1日 | 運用用の health/readiness と手順書を作る | health endpoint / readiness / runbook（新規） | P5-09, P7-06, P9-02 | 起動確認、DB 接続確認、ORCA 接続確認、ストレージ確認、キュー詰まり確認の手順をそろえる。障害一次切り分けを誰でもできるようにする。 | 運用担当が同じ観点で確認できる。 | runbook、health 実装、確認手順 | まず始業前確認と障害時確認の2章構成で書く。 |
| ☑ | P10-01 | 10. 移行と本番切替 | S | 基盤担当 + DB担当 | 2日 | 検証環境を本番に近い形で立てる | 新 module 一式 / 外部設定 / DB / ORCA 接続先 / ストレージ | P5-08, P6-08, P8-04, P9-06 | 本番相当の設定で刷新版を動かす環境を作る。ローカルだけで確認せず、外部設定とデータ移行を含めて試せる場にする。 | 本番に近い条件で総合試験が行える。 | 検証環境、構築手順、接続情報管理手順 | まず秘密情報を除いた構築手順から書き起こす。 |
| ☑ | P10-02 | 10. 移行と本番切替 | S | DB担当 + QA | 2日 | データ移行の通し試験を行う | 移行スクリプト / 検証 DB / 件数照合 | P6-09, P10-01 | 初期化、移行、件数照合、簡易業務確認までを通しで行う。途中で止まったときの再実行手順も確認する。 | 移行手順が再現でき、件数差異を説明できる。 | 移行試験記録、差異一覧、修正点 | 1 回目は時間計測よりも差異確認を優先する。 |
| ☑ | P10-03 | 10. 移行と本番切替 | S | 業務担当 + QA | 2日 | 業務受け入れ試験を行う（モダナイズ版単体） | 患者、カルテ、受付、ORCA、添付、管理の主要フロー | P10-01, P10-02 | 旧サーバー比較を前提にせず、モダナイズ版単体で主要操作を通す。実運用ロールが揃わない場合は仮想ロール（医師/受付/事務）台本で代替する。 | モダナイズ版単体で主要業務フローが通り、差し戻し点が整理される。 | UAT 記録、指摘一覧、修正方針 | 役割別台本と期待結果を先に固定する。 |
| ☑ | P10-04 | 10. 移行と本番切替 | A | 基盤担当 | 1.5日 | 負荷試験と障害試験を行う | カルテ保存 / 患者検索 / ORCA 連携 / 添付保存 / worker | P5-09, P6-10, P10-01 | 混雑時の応答、外部遅延時の挙動、ストレージ一時障害時の動きを確認する。本番前に限界と安全側の設定値を把握する。 | 受け入れ可能な応答時間と障害時挙動が分かる。 | 負荷試験記録、設定値、改善項目 | まず単一経路ごとの負荷を測り、その後に複合負荷をかける。 |
| ☑ | P10-05 | 10. 移行と本番切替 | S | アーキテクト | 1日 | 本番切替チェックリストを作る（モダナイズ版基準） | runbook / DB backup / 秘密情報 / 監視設定 / 連絡体制 | P10-02, P10-03, P10-04 | 旧サーバーとの差分比較ではなく、モダナイズ版の正常稼働確認項目（health/readiness、主要業務疎通、監視）を中心に時系列手順を作る。 | モダナイズ版の稼働確認を順番通り実施できる。 | 本番切替手順書、役割分担表 | P10-03/P10-04 の結果をチェックリストへ反映する。 |
| ☑ | P10-06 | 10. 移行と本番切替 | S | 全担当 | 0.5日 | 本番切替を実施する（モダナイズ版稼働確認） | 本番環境全体 | P10-05 | 決めた手順に従って起動・接続・主要業務疎通・監視確認を実施する。旧サーバー比較は要求しない。 | モダナイズ版が正常稼働し、主要業務と監視が正常である。 | 切替記録、当日ログ、引継ぎメモ | 稼働確認結果をその場でチェックリストへ反映する。 |
| ☐ | P10-07 | 10. 移行と本番切替 | A | 全担当 | 3日 | 切替後の集中監視と是正を行う | 運用監視 / エラーログ / 問い合わせ / ORCA 連携結果 | P10-06 | モダナイズ版稼働後のエラー・性能・問い合わせを重点監視し、日次で是正する。旧サーバー比較は行わず、現行稼働の健全性で判定する。 | 切替後の不具合収束状況が見え、恒常運用へ移れる。 | 集中監視記録、是正一覧、クローズ条件 | 1日目、2日目、3日目の確認項目を分けて運用する。 |

## ブロッカー
- 2026-03-13 (RUN_ID=20260313T054324Z, 解消済み): `P10-06` を完了。`PVTResource` の JSON 変換を serialize mapper 基準へ統一して `POST /resources/pvt` の `LocalDateTime` 復元を修正し、`KarteServiceBean` / `ChartEventServiceBean` の `LocalDateTime` bind 不整合と Hibernate 6 向け HQL property 名ずれ、`DocumentIntegrityService` の新規 row persist 順序不備、患者画像 upload の digest 未設定を補正。validation 環境へ再配備後、`/api/session/login`、`/health/readiness`、`/karte/documents/9102003`、`/karte/attachment/9105001`、`/karte/image/9104001`、`/orca/medical/records`、`/pvt`、`/orca/order/bundles`、`/orca/patient/mutation(create/update)`、`/api/admin/access/users`（admin=200 / unauthenticated=401）を実測し、主要業務疎通と監視確認を完了。`P10-07` は集中監視タスクとして次回先頭未着手へ引き継ぐ。
- 2026-03-13 (RUN_ID=20260312T234207Z, 未解消): `P10-06` を継続再試行。Docker daemon 復旧後に RUN 専用 compose project `opendolphin_prodcutover_20260312t234207z` で validation 環境を起動し、host build の `opendolphin-server.war` を RUN 専用 server container へ差し替えて再起動。Flyway/search_path 補正、`AuditTrailService` の native SQL 化、`InitialAccountMaker` の `search_path` 設定、`AuditEvent` の `persistence.xml` 登録、`LoginAttemptPolicyService` の `REQUIRES_NEW` 化、seed password の PBKDF2 化を反映後、`GET /openDolphin/` は **200** で CSRF token 発行、`POST /openDolphin/resources/api/session/login` は CSRF/Origin 条件を満たすと **500 → 401** まで改善。`opendolphin.d_users` には PBKDF2 hash の sysad/doctor ユーザーが存在し、`PasswordHashService` 単体検証は PASS だが、実ランタイムの session login は `LOCAL.FACILITY.0001:dolphin` / `1.3.6.1.4.1.9414.72.103:doctor1` とも **401 unauthorized** のままで、`/resources/dolphin`・`/resources/health/readiness` の authenticated 確認へ進めない。`P10-06` は主要業務疎通の完了条件未達。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-13 (RUN_ID=20260312T233406Z, 未解消): `P10-06` を再試行。`start-validation-env.sh` 用に `server-modernized.production.env.sample` から `/tmp/server-modernized.production.20260312T233406Z.env` を生成し、`COMPOSE_PROJECT_NAME`/`SERVER_CONTAINER_NAME`/`MODERNIZED_APP_HTTP_PORT` を上書き。`curl --max-time 2 --unix-socket /Users/Hayato/.docker/run/docker.sock http://localhost/_ping` は **RC=28**（`2.004s`）でタイムアウト。`docker daemon is not responding ... aborting before compose up` で **RC=1**（fail-fast）となり `compose up` 到達前に停止。`docker context ls` は表示成功だが、`default` と `desktop-linux` は同一 socket 前提で進めたため本質的に改善なし。`P10-06` は完了条件（起動→`health/readiness`→主要業務疎通）に到達せず。
- 2026-03-13 (RUN_ID=20260312T210043Z, 未解消): `P10-06` を再試行。`curl --max-time 8 --unix-socket /Users/Hayato/.docker/run/docker.sock http://localhost/{_ping,version}` は今回も RC=28 timeout。`/var/run/docker.sock`（`/Users/Hayato/.docker/run/docker.sock` への symlink）経由の `/_ping` も RC=28 timeout。`docker context ls` は RC=0 で context 一覧取得自体は可能だが daemon 応答改善はなし。`server-modernized.production.env.sample` から `/tmp/server-modernized.production.20260312T210043Z.env` を生成し、`DOCKER_PING_TIMEOUT_SECONDS=2 COMPOSE_PROJECT_NAME=opendolphin_prodcutover_20260312t210043z ... start-validation-env.sh ...` を `/Users/Hayato/.docker/run/docker.sock` と `/var/run/docker.sock` の両経路で実行したが、いずれも fail-fast（RC=1）で `compose up` 前停止。daemon 応答不可のため `P10-06` 完了条件（起動→`health/readiness`→主要業務疎通）には未到達。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-13 (RUN_ID=20260312T200042Z, 未解消): `P10-06` を再試行。`curl --max-time 8 --unix-socket /Users/Hayato/.docker/run/docker.sock http://localhost/{_ping,version}` は今回も RC=28 timeout。`/var/run/docker.sock`（`/Users/Hayato/.docker/run/docker.sock` への symlink）経由の `/_ping` も RC=28 timeout。`server-modernized.production.env.sample` から `/tmp/server-modernized.production.20260312T200042Z.env` を生成し、`DOCKER_PING_TIMEOUT_SECONDS=2 COMPOSE_PROJECT_NAME=opendolphin_prodcutover_20260312t200042z ... start-validation-env.sh ...` を実行したが fail-fast（RC=1）で `compose up` 前停止。daemon 応答不可のため `P10-06` 完了条件（起動→`health/readiness`→主要業務疎通）には未到達。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-13 (RUN_ID=20260312T190035Z, 未解消): `P10-06` を再試行。`curl --max-time 8 --unix-socket /Users/Hayato/.docker/run/docker.sock http://localhost/{_ping,version}` は今回も RC=28 timeout。`server-modernized.production.env.sample` から `/tmp/server-modernized.production.20260312T190035Z.env` を生成し、`DOCKER_PING_TIMEOUT_SECONDS=2 ... start-validation-env.sh ...` を実行したが fail-fast（RC=1）で `compose up` 前停止。daemon 応答不可のため `P10-06` 完了条件（起動→`health/readiness`→主要業務疎通）には未到達。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-13 (RUN_ID=20260312T180123Z, 未解消): `P10-06` を再試行。`curl --max-time 8 --unix-socket /Users/Hayato/.docker/run/docker.sock http://localhost/{_ping,version}` は引き続き RC=28 timeout。`docker info` と `docker --context default info` は 12秒 watchdog でいずれも RC=1（Server は `context canceled`）。`server-modernized.production.env.sample` から `/tmp/server-modernized.production.20260312T180123Z.env` を生成して `DOCKER_PING_TIMEOUT_SECONDS=2 ... start-validation-env.sh ...` を実行したが fail-fast（RC=1）で `compose up` 前停止。daemon 応答不可のため `P10-06` 完了条件（起動→`health/readiness`→主要業務疎通）には未到達。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-13 (RUN_ID=20260312T170046Z, 未解消): `P10-06` を再試行。`curl --max-time 8 --unix-socket /Users/Hayato/.docker/run/docker.sock http://localhost/{_ping,version}` は引き続き RC=28 timeout。`docker info` と `docker --context default info` は 12秒 watchdog で RC=124（server 側 `context canceled`）。`/var/run/docker.sock` も同一 socket への symlink で `/_ping` は RC=28 timeout。`server-modernized.production.env` を sample から作成して `DOCKER_PING_TIMEOUT_SECONDS=2 ... start-validation-env.sh ...production.env` を試行したが fail-fast（RC=1）で `compose up` 前停止。daemon 応答不可のため `P10-06` 完了条件（起動→`health/readiness`→主要業務疎通）には未到達。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-13 (RUN_ID=20260312T160044Z, 未解消): `P10-06` を再試行。`curl --max-time 8 --unix-socket /Users/Hayato/.docker/run/docker.sock http://localhost/{_ping,version}` は継続して RC=28 timeout。`docker info` / `docker --context desktop-linux info` / `docker --context default info` も 12秒 watchdog で RC=124（server 側 `context canceled`）となり daemon 応答なしを再確認。`DOCKER_PING_TIMEOUT_SECONDS=2 ... start-validation-env.sh ...validation.env.sample` は想定どおり fail-fast（RC=1）したが、`server-modernized.production.env` は未配置のままのため `P10-06` 完了条件（起動→`health/readiness`→主要業務疎通）には未到達。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-12 (RUN_ID=20260312T150045Z, 未解消): `P10-06` を再試行。`curl --max-time 8 --unix-socket /Users/Hayato/.docker/run/docker.sock http://localhost/{_ping,version}` はいずれも RC=28 timeout で Docker daemon 応答が得られず、`up/build/ps` 実行が継続不能。`ops/modernized-server/scripts/start-validation-env.sh` に daemon preflight（socket存在確認 + `/_ping` 応答確認）を追加し、ハング前に即時失敗させる導線へ改善したが、`server-modernized.production.env` も未配置のため `P10-06` 完了条件（起動→`health/readiness`→主要業務疎通）には未到達。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-12 (RUN_ID=20260312T140055Z, 未解消): `P10-06` を再試行。`server-modernized.production.env` を sample から作成し、`docker compose ... config` は PASS。`COMPOSE_PROJECT_NAME=opendolphin_prodcutover_20260312t140055z ops/modernized-server/scripts/start-validation-env.sh ...production.env` 実行時に `starting containers...` 以降がハングしたため、watchdog 付きで再計測したところ `docker info` は `Server: context canceled`、`docker compose ... ps` は RC=130 timeout、`curl --unix-socket /Users/Hayato/.docker/run/docker.sock http://localhost/_ping` は RC=28 timeout。Docker daemon 応答不安定により本番切替実行（`up/build` → `health/readiness` 実測）へ進めず。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-12 (RUN_ID=20260312T130107Z, 未解消): `P10-06` を再試行。`LegacyObjectMapperProducer` を `@Dependent` 化し、`docker` build 導線も `ops/modernized-server/docker/Dockerfile` で `domain/api-contract/persistence` コピー + `pom.server-modernized.xml` 固定利用へ補正。`/workspace/persistence` 欠落および `opendolphin-api-contract` 欠落エラーは解消したが、`docker compose ... build server-modernized-dev` が依存解決フェーズで長時間化し実行時間内に image build 完了まで到達できず。`server-modernized.production.env` も未配置のため `P10-06` は継続未完。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-12 (RUN_ID=20260312T120057Z, 未解消): `P10-06` を再試行。validation compose を RUN_ID 固有 container 名で起動可能化し、`up --no-build` で DB/MinIO/server 起動までは成功したが、`/openDolphin/resources/{dolphin,health,health/readiness}` が 404。`docker logs` で `WELD-001480` / `WELD-001410`（`LegacyObjectMapperProducer` の proxy 不可）により `opendolphin-server.war started (with errors)` を確認。本番用 env（`server-modernized.production.env`）も未配置のため `P10-06` は継続未完。詳細は `docs/modernization/p10-06-cutover-execution-blocker.md` を参照。
- 2026-03-12 (RUN_ID=20260312T110053Z, 未解消): `P10-06` 本番切替実施は、本番用 env（`server-modernized.production.env`）未配置かつ実行環境の Docker buildx 権限不足（`operation not permitted`）で実行不能。`docs/modernization/p10-06-cutover-execution-blocker.md` に試行結果と次回着手条件を記録。
- 2026-03-12 (RUN_ID=20260312T100053Z, 計画前提更新): 旧サーバーは稼働していないため、`P10-03` 以降は「モダナイズ版単体が正常稼働すること」を完了判定の前提に更新。比較検証は必須条件から除外する。
- 2026-03-12 (RUN_ID=20260312T100053Z, 運用方針更新): `P10-03` は実運用ロール不在時でも、仮想ロール（医師/受付/事務）を明示したUAT台本を作成し、単独担当でロール切替実施する方式で前進してよい。次回ワーカーはこの方式で `P10-03` を実施し、指摘一覧を `P10-05` 入力へ接続すること。
- 2026-03-12 (RUN_ID=20260312T110053Z, 解消済み): `P10-03` を仮想ロールUAT方式で実施。`docs/modernization/p10-03-virtual-role-uat.md` に医師/受付/事務の台本、期待結果/実結果、差分分類（必須修正/改善候補）を記録し、主要フロー回帰（42 tests）PASS を再確認したため完了判定とした。
- 2026-03-12 (RUN_ID=20260312T100053Z, 未解消): `P10-03` 業務受け入れ試験の完了条件（医師/受付/事務の現場観点 UAT）を再試行観点で再評価したが、実運用ロールによる受け入れ実査をこのRUN単独では実施できず未解消。技術回帰テスト PASS のみでは完了判定不可のため、`P10-03` は継続して未完了。
- 2026-03-12 (RUN_ID=20260312T090057Z, 未解消): `P10-03` 業務受け入れ試験について、技術事前確認として主要フロー回帰テスト（患者/カルテ/PVT/ORCA/添付/管理認証）を実行し 42 tests PASS を確認したが、完了条件である「医師・受付・事務の現場観点UAT」は本RUN単独では実施不可。API/ユニットテスト合格のみでは UAT 完了判定とならないため `P10-03` は未完了のまま保持。
- 2026-03-12 (RUN_ID=20260312T090057Z, 解消済み): `P10-02` データ移行通し試験を専用DBコンテナ（`opendolphin-postgres-modernized-e730-20260312t090057z`）で再実施し、初期化→移行→件数照合→再実行確認を完了。`run-module-payload-migration.sh` の `d_module_payload_migration_run.after_*` 集計が同一 statement スナップショット起因で不整合になる差異を検知し、`module-payload-migrate-once.sql` を upsert 後の別 `UPDATE` 再計算へ修正して解消。修正後の `r1/r2` は `after_payload_rows=2` / `after_missing_rows=0` で整合。
- 2026-03-12 (RUN_ID=20260311T200758Z, 解消済み): `P6-04` を `medOrder`/`progressCourse` の versioned envelope（`schemaVersion`/`moduleType`/`payloadJson`/`payloadHash`）導入で完了。`ModuleJsonConverter` に新形式の encode/decode を実装し、`KarteServiceBean`・`KarteDocumentWriteService`・`OrcaOrderBundleResource`・`OrcaSubjectiveResource`・`OrcaResource` の module 書込経路を `ModelUtils.encodeModule(...)` に統一した。`P6-08` は新 schema / migration の適用タスクとして後続実施に整理。
- 2026-03-12 (RUN_ID=20260311T190124Z, 未解消): `P6-04` の実装成立可否を再試行。 (1) `P6-04` と `P6-08` を同一実行内で同時実装して依存を内包する案、(2) `d_module_payload` 先行導入 + 既存 `bean_json` 併用で段階移行する案、(3) 主要2種（`medOrder`/`progressCourse`）のみ新形式へ先行切替する案を比較した。`d_module.bean_json` 依存が `server-modernized`/`persistence` に少なくとも 47 箇所残存し、`OrcaOrderBundleResource` の `SELECT CAST(bean_json AS text) FROM d_module` 直接参照も存在するため、(1) は `P6-08` の完了条件（新規/既存DB両系統で migration 成功）まで同時に満たせず未完、(2)(3) は WBS 完了条件（旧保存形式依存なし）に反するため採用不可。WBS順序（`P6-04`→`P6-08`）と依存（`P6-04` depends on `P6-08`）の矛盾が継続しており、運用判断（順序変更または `P6-08` 先行例外）が必要。
- 2026-03-12 (RUN_ID=20260311T180746Z, 未解消): `P6-04` について解消案を再試行。 (1) `P6-04` 内で `d_module_payload` の暫定DDLを先行投入して後続で `P6-08` に再分離、(2) `P6-08` を先行着手して `P6-04` の前提を満たすよう順序を逆転、(3) `bean_json` を維持したまま `P6-04` の先行コード（mapper/repository骨格）のみ先に実装、を比較した。結果は、(1) は migration 履歴の二重管理と巻き戻し不能リスク、(2) は「上から順実行」違反、(3) は WBS完了条件（保存/取得/改訂/比較を新形式で通す）を満たせないため、いずれも採用不可。運用判断（WBS順序の変更許可または `P6-08` 先行の例外許可）が必要。
- 2026-03-12 (RUN_ID=20260311T170115Z, 未解消): `P6-04` は依存に `P6-08`（新 schema Flyway migration）を含むが、WBSの上から順実行では `P6-04` が先に来るため完了条件を満たせない。解消案として (1) `P6-04` 内で暫定DDLを先行実装して後で `P6-08` に再分離、(2) `P6-08` を先行着手して依存解消、を比較したが、前者は migration 履歴二重化リスク、後者は「上から順実行」違反になるため採用不可。WBS順序または依存関係のどちらを優先するかの運用決定が必要。
- 2026-03-11 (RUN_ID=20260311T053558Z, 解消済み): `P2-06` の先行ブロッカーだった `web-client` の XML POST 直接依存を解除。`web-client/src/libs/http/httpClient.ts` に ORCA XML POST の `/api/v1/orca/bridge` 自動転送を追加し、`server-modernized/src/main/java/open/dolphin/rest/OrcaBridgeResource.java` で ORCA transport へ集約転送する構成へ変更。依存整理は `docs/modernization/p2-06-xml-endpoint-blocker.md` を参照。
- 2026-03-11 (RUN_ID=20260310T230817Z, 解消済み): `P1-09` の追加テスト実行時、ローカルの JDK 25 環境で Mockito inline mock maker が自己 attach できず (`AttachNotSupportedException`)、`AdminAccessResourceTest` / `AdminOrcaConnectionResourceTest` / `SessionAuthResourceTest` / `LogoutResourceTest` が実行不能。`-Djdk.attach.allowAttachSelf=true -XX:+EnableDynamicAgentLoading` を付与しても解消せず、暫定で JDK21 + byte-buddy-agent 手順を採用。
- 2026-03-11 (RUN_ID=20260310T232050Z): `master` 上で同一テスト群（`AdminAccessResourceTest` / `AdminOrcaConnectionResourceTest` / `SessionAuthResourceTest` / `LogoutResourceTest`）を JDK25(Homebrew) 既定設定で再実行し、25 tests PASS を確認。現行方針を **JDK25既定（fallback: JDK21 + byte-buddy-agent）** に確定。
