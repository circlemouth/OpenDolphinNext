confirmed_accessible_file:
  - /mnt/data/dads_app_ui_design_rules_20260411.md

inline_context:
  task_name: correction-note / setting-note verification
  known_filename:
    - e2e-billing-correction-note.spec.ts
  reason_for_task:
    - touched by the recent work
    - not included in targeted retests
    - worker report calls out separate contract confirmation need

repo_touchpoints:
  - e2e-billing-correction-note.spec.ts (actual path must be found in repo)

required_tests:
  - single-spec re-run for the actual correction-note spec path

do_not_invent:
  - actual path before repo search
  - failure classification before the spec is run
  - relation to existing closed blockers without repo evidence
