confirmed_accessible_file:
  - docs/web-client/ux/dads_app_ui_design_rules_20260411.md

inline_context:
  already_closed_blockers:
    - Reception transmission projection fix: pass
    - OrcaSummary mount contract fix: pass
    - Print preview harness-first isolation/fix: pass
  targeted_retests_passed:
    - tests/reception/e2e-rec-001-status-mvp.spec.ts
    - tests/charts/e2e-orca-billing-status.spec.ts
    - tests/e2e/charts-report-print.msw.spec.ts
  remaining_release_gate_items:
    - correction-note contract verification still needed because touched spec was not retested
    - runtime-ready-smoke failed with ECONNREFUSED at 127.0.0.1:9080
    - web-client npm run ci failed with 20 failures across 5 files
    - manual QA not yet done
    - ORCA live QA not yet done
  fixed_premises:
    - 3 pane responsibilities fixed
    - patient context is non-persistent
    - finish and send are separated
    - right rail is chooser-only
    - sent and paid are not merged
    - send success != paid
    - no new generic bottom navigation
    - important information must not be hidden in disclosure
    - one screen one primary
    - unknowns remain gated with fail-close fallback
    - backward compatibility not required
    - ignore build artifacts, logs, screenshots, test-results, and worktree artifacts

repo_touchpoints:
  - web-client/src/features/charts/__tests__/chartsOrderDockCoexistence.recovery-order.test.tsx
  - web-client/src/features/charts/__tests__/orderBundleOrcaSupport.test.tsx
  - web-client/src/features/charts/__tests__/prescriptionOrderEditorPanel.orca-support.test.tsx
  - web-client/src/features/charts/__tests__/prescriptionOrderOrcaSupport.test.tsx
  - e2e-billing-correction-note.spec.ts (actual path must be located in repo)
  - web-client/scripts/runtime-ready-smoke.mjs
  - web-client/playwright.config.ts
  - web-client/src/features/charts/orcaMaster.ts

required_tests:
  - correction-note spec re-run after locating actual path
  - cd web-client && node scripts/runtime-ready-smoke.mjs
  - cd web-client && npm run ci
  - mvn -f pom.server-modernized.xml -pl server-modernized -am -Pstatic-analysis verify

do_not_invent:
  - fifth failing CI file before actual ci output shows it
  - backend start command before repo evidence shows it
  - health endpoint or readiness endpoint names before repo evidence
  - correction-note spec path before repo search confirms it
  - any reopening of Task 31 without new repo evidence
