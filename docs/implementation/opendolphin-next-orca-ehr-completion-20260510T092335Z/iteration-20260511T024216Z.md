# Worker E Iteration 20260511T024216Z

## Scope
- Queue: E-04 follow-on.
- Checklist: 11 disabled dependency reason display; 20 Phase 4 Medical Safety UI / DADS.
- Surface: `DoCopyDialog` apply control.

## Threat Model
- Asset: patient-scoped SOAP draft and Do-copy workflow.
- Trust boundary: client-side selected sections are advisory UI state; downstream draft application still validates selected sections in the page handler.
- Attack surface: disabled apply control when there is no transferable source or no selected target section.

## Misuse Cases
- A user deselects all Do target sections and sees `適用` disabled without knowing what to do.
- Assistive technology reaches the disabled apply action without an associated reason.
- A future regression removes the reason while leaving the disabled state.

## Implementation
- Kept native disabled for apply when no source exists or no section is selected.
- Added nearby `charts-do-copy-apply-block-reason` with the missing requirement.
- Connected the disabled apply button with `aria-describedby`.

## Verification
- `cd web-client && npm test -- --run src/features/charts/__tests__/DoCopyDialog.test.tsx`
- `cd web-client && npm run verify:web-guard`
- `cd web-client && npm run typecheck`
- `git diff --check`

## Docs
- Updated `web-client/notes/ui-current-contract.md`.
- Updated `worker-board.md`.
- Updated `opendolphin-next-orca-ehr-implementation-checklist.md` only for the verified item.

## Residual Risk
- A broader disabled-surface audit remains for non-OrderDock Medical Safety UI controls.

## Next
- Continue E-04 remaining Medical Safety UI disabled surface audit, then move to checklist 17 remaining items if no E-04 issues remain.
