# Worker E Iteration 20260511T022215Z

## Scope
- Queue: E-04 follow-on.
- Checklist: 11 disabled dependency reason display; 20 Phase 4 Medical Safety UI / DADS.
- Surface: `OrderRecommendationModal` category scope selector.

## Threat Model
- Asset: patient-scoped frequent-order recommendation workflow.
- Trust boundary: modal state and selected entity are client-side convenience only; order mutation validation remains server-side and editor-side.
- Attack surface: disabled category scope when the modal has no default entity.

## Misuse Cases
- A user sees `このカテゴリ` disabled and cannot tell why the category view is unavailable.
- Assistive technology reaches the disabled category scope without a related explanation.
- A future UI change leaves category scope disabled without telling users to use the cross-category scope.

## Implementation
- Kept category scope native disabled when there is no default entity.
- Added nearby `order-recommend-category-scope-reason` explaining the missing category and the `横断` alternative.
- Connected the disabled category scope button to the reason with `aria-describedby`.

## Verification
- `cd web-client && npm test -- --run src/features/charts/__tests__/orderRecommendationModal.disabledReason.test.tsx`
- `cd web-client && npm run verify:web-guard`
- `cd web-client && npm run typecheck`
- `git diff --check`

## Docs
- Updated `web-client/notes/ui-current-contract.md`.
- Updated `worker-board.md`.
- Updated `opendolphin-next-orca-ehr-implementation-checklist.md` only for the verified item.

## Residual Risk
- A broader disabled-surface audit remains for non-OrderDock Medical Safety UI controls.

## Next
- Continue E-04 remaining Medical Safety UI disabled surface audit, then move to checklist 17 remaining items if no E-04 issues remain.
