# Worker A Iteration 20260511T101041Z

## Scope

- Worker: A - ORCA patient / acceptance / insurance boundary and live readiness.
- Selected item: R2-A01 official appointment list/patient GET routes.
- Checklist: 3.3 / 6 / 13, specifically `GET /api/orca/official/appointments/list?date=...` and `GET /api/orca/official/appointments/patient?...`.

## Initial State

- Worktree: `/Users/Hayato/.codex/worktrees/opendolphinnext-orca-ehr-r2-worker-a/OpenDolphin_WebClient`
- Branch: `codex/orca-ehr-r2-worker-a-orca-boundary-live`
- Base HEAD: `7472d741f`
- Initial `git status --short`: clean

## Assets / Trust Boundary / Attack Surface

- Assets: official ORCA appointment/acceptance read routes, server-side facility resolution, route taxonomy, audit details, live readiness contract.
- Trust boundary: query/body values are client input; facility, ORCA endpoint, auth context, route scope, and audit authority remain server-derived.
- Attack surface: malformed date query, patient id query misuse, facility/URL/role smuggling through query, legacy/local route drift, exception detail leakage.

## Misuse Cases

1. Client includes facility/URL/role-like query data to steer ORCA transport. Mitigation: GET wrappers only map allowlisted query fields into existing DTOs; facility still comes from authenticated request context.
2. Malformed date query triggers parser exception detail leakage. Mitigation: date parsing fails before ORCA transport with fixed `orca.appointment.invalid` and sanitized audit detail.
3. Official appointment route remains POST-only while docs/checklist require GET, encouraging ad-hoc local aliases. Mitigation: route inventory now includes GET official list/patient routes and tests reject taxonomy drift.

## Implementation

- Added `GET /api/orca/official/appointments/list` query wrapper with `date` / `appointmentDate` / `fromDate` / `toDate` support, normalized into the existing official wrapper path.
- Added `GET /api/orca/official/appointments/patient` query wrapper with `patientId`, optional `baseDate`, and `departmentCode`, normalized into the existing official patient appointment wrapper.
- Added fixed date parsing and sanitized failure audit for query routes.
- Updated route inventory tests, appointment resource focused tests, route taxonomy docs, checklist, and worker board.

## Verification

- `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=OrcaAppointmentResourceTest,PublicRouteInventoryContractTest,WebXmlEndpointExposureTest test` - passed.
- `bash server-modernized/tools/ci/check-doc-links.sh` - passed.
- `bash server-modernized/tools/ci/check-config-contract.sh` - passed.
- `bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"` - passed.
- `bash server-modernized/tools/ci/check-sensitive-evidence-redaction.sh --root "$(git rev-parse --show-toplevel)"` - passed.

## Residual Risk / Next Task

- This heartbeat does not implement encounter-to-ORCA acceptance link tables or UI warning/diff display.
- Next Worker A task: R2-A02 encounter/acceptance link and stale patient/insurance/acceptance warning gaps.
