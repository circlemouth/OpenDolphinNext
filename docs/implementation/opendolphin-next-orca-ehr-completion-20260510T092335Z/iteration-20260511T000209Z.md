# Worker C Iteration 20260511T000209Z

## Scope

- Worker: C
- Queue item: C-06 prescription authority send/re-fetch handoff review
- Checklist: 8, 10.2 handoff hardening; no checklist item newly closed
- Branch: `codex/orca-ehr-worker-c-prescription`
- Worktree: `/Users/Hayato/.codex/worktrees/opendolphinnext-orca-ehr-worker-c/OpenDolphin_WebClient`

## Assets / Trust Boundary / Attack Surface

- Assets: `orca_medical_candidate` response, `ChartSupportMedicalModV2Request.MedicalInformation`, Charts candidate API parser.
- Trust boundary: client calls candidate prepare with only `chartRevisionId`; facility, patient, encounter, and prescription revision are resolved server-side.
- Attack surface: `/api/local/orca/medical-candidates/from-chart/{chartRevisionId}` and Charts candidate prepare client.

## Misuse Cases

- Client tries to supply facility, patient, insurance combination, voucher, sequential, URL, digest, owner, or role during candidate prepare.
- Candidate handoff omits `usageCode`, forcing live send code to infer 用法 from client display text or stale browser state.
- Candidate is mistaken for ORCA source-of-truth or send completion despite being non-authoritative.
- Missing usage code is hidden in candidate rows and reaches live send as if sendable.

## Implementation

- Added `usageCode` and `usageName` to `ChartSupportMedicalModV2Request.MedicalInformation`.
- Updated `LocalOrcaMedicalCandidateResource` to populate usage fields from saved prescription revision and continue to mark missing usage code as `NEEDS_REVIEW`.
- Updated Charts `orcaMedicalCandidateApi` parser/type and focused test so web code preserves the first-class usage handoff fields.
- Documented the handoff rule in `docs/contracts/prescription-authority.md` and `web-client/notes/ui-current-contract.md`.

## Verification

- PASS: `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=LocalOrcaMedicalCandidateResourceTest test`
- PASS: `cd web-client && npm test -- --run src/features/charts/__tests__/orcaMedicalCandidateApi.test.ts`

## Docs Updated

- `docs/contracts/prescription-authority.md`
- `web-client/notes/ui-current-contract.md`
- `docs/implementation/opendolphin-next-orca-ehr-completion-20260510T092335Z/worker-board.md`

## Residual Risk / Next

- Live `medicalmodv2` send, post-send ORCA re-fetch, and diff reconciliation remain Worker D scope.
- C-07 should stay as an integration watch task for Worker C-owned DTO/source-of-truth conflicts after Worker D connects send/re-fetch.
