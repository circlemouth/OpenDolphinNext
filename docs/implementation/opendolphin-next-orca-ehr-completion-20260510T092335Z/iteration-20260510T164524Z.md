# Iteration 20260510T164524Z

## Checklist Item

- 16.4 follow-up: DB障害時読み取り専用モードを処方指示 mutation に接続する。

## Scope

- `/api/local/prescription-orders` の保存と `/api/local/prescription-orders/do-import` の DO 取込を、監査ログ write path が利用できない場合に fail-closed にする。
- 処方 payload 永続化前に `audit_log_write_unavailable` / HTTP 503 を返し、監査不能な処方変更を進めない。

## Threat Model

- DB degraded / read-only / restore 中に処方指示だけが保存され、監査ログ hash chain に操作証跡が残らない。
- DO 取込で処方 payload が上書き相当に追加保存され、監査不能な変更として後から真正性を説明できない。
- client が患者IDや encounterId を送っても、処方 mutation の可否は server-side audit write path によって決まるべき。

## Changes

- `LocalPrescriptionOrderResource`
  - `AuthoritativeAuditRepository` を注入。
  - `saveOrder` と `doImport` の入口で `isWritePathAvailable()` を確認し、不可なら `audit_log_write_unavailable` の sanitized 503 を返す。
- `LocalPrescriptionOrderResourceTest`
  - audit write path unavailable 時に保存/DO取込が永続化前に止まる regression test を追加。
- `opendolphin-next-orca-ehr-implementation-checklist.md`
  - 処方 mutation の audit write path fail-closed を完了サブ項目として記録。

## Verification

- PASS: `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=LocalPrescriptionOrderResourceTest test`
- PASS: `bash server-modernized/tools/ci/check-doc-links.sh`
- PASS: `bash server-modernized/tools/ci/check-config-contract.sh`
- PASS: `bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"`
- PASS: `cd web-client && npm run verify:web-guard`
- PASS: `git diff --check`

## Residual Risk

- 診療録本文保存、訂正/追記/取消、添付操作への read-only mode 強制は次工程。
- `GET /api/local/prescription-orders` は read-only 取得のため今回の fail-closed 対象外。
