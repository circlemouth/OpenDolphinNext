# Iteration 20260511T141708Z - Worker F Full Release Gate Refresh

## Scope

- Worker F R2-F07: full release gate refresh for web CI and server Maven static-analysis verify.
- Checklist coverage: 15/18/19 release gate, browser secret exposure, server static-analysis, and blocker record.
- No A-D business implementation, E UI redesign, `client/`, or legacy `server/` changes.

## Threat Model

- Assets: release gate pass/fail status, CI command output, ignored local runtime env, worker board summary, and reviewer-facing blocker record.
- Trust boundary: raw command output may exist only in the local terminal; tracked docs record only command names, pass/fail status, fixed blocker categories, and sanitized counts.
- Attack surface: local `.env.local`, web guard output, Maven logs, worker board, iteration docs, and any release evidence copied into reviewer packets.

## Misuse Cases

1. A web CI failure caused by local ORCA transport keys in `.env.local` is misclassified as a tracked source defect.
2. Maven static-analysis output with warnings is summarized as passing without checking SpotBugs result counts.
3. Passing web/server full gates are used to bypass live Trial exact preflight, runtime-ready, or mutation-ready candidate requirements.
4. Command output containing credentials, raw ORCA body, PHI, HAR, trace, video, or screenshot paths is copied into tracked evidence.

## Implementation

- Ran the full web release gate. The first run stopped at `verify:no-direct-orca-proxy-config` because ignored local `web-client/.env.local` still contained client-side ORCA transport keys.
- Removed only the ignored local `VITE_ORCA_MODE` and `VITE_ORCA_API_PATH_PREFIX` entries from `web-client/.env.local`; no tracked env/config file was changed.
- Reran `web-client npm run ci` successfully.
- Ran server Maven static-analysis verify successfully and confirmed SpotBugs `BugInstance size is 0`.
- Updated the workstream checklist and worker board with the full gate evidence while keeping live Trial mutation/readiness blockers open.

## Verification

- `cd web-client && npm run ci` passed.
  - `verify:web-guard` passed.
  - TypeScript typecheck passed.
  - Vitest passed: 212 files, 1564 passed, 2 skipped.
  - Vite production build passed; build warning was only the existing large chunk warning.
- `mvn -f pom.server-modernized.xml -pl server-modernized -am -Pstatic-analysis verify` passed.
  - Server tests passed.
  - Failsafe integration tests passed.
  - SpotBugs reported `BugInstance size is 0`.

## Result

- The previous server static-analysis blocker is not present on this Worker F branch.
- The web CI blocker was local ignored env drift, not a tracked source defect; after removing client-side ORCA transport keys from `.env.local`, full web CI passed.
- Remaining blocker is still `no_trial_native_mutation_ready_candidate`: runtime-ready / exact selected-candidate preflight / mutation execution must remain blocked until local selectable, selector availability, and runtime-ready preconditions are fixed.
