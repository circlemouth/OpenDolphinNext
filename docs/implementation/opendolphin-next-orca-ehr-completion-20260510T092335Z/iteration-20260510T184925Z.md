# Iteration 20260510T184925Z

## Checklist Item

- Section 17: ORCA送信成功を診療録確定と同義に扱う UI 文言、ORCA送信失敗時に登録済み/反映済みと表示する文言、重要警告を初期非表示にする UI を削除または変更する。

## Threat Model

- Misuse case 1: Patients の `missingMaster` / `fallbackUsed` を `通信詳細` disclosure 内だけで確認する設計になり、ORCA正本確認不能のまま患者基本情報を編集できると誤認する。
- Misuse case 2: 非 server/local dataSource transition の警告が運用メタ扱いに見え、利用者が復旧前に患者更新または ORCA既存患者取込を進める。
- Misuse case 3: `状態: 要注意` だけでは原因と次アクションが分からず、詳細を開かない利用者が同期停止の理由を見落とす。

## Changes

- Patients の同期停止理由を `patients-page__ops-alert` としてヘッダー直下に初期表示した。
- `missingMaster`、`fallbackUsed`、非 server/local transition、患者一覧取得失敗の理由を disclosure 外の visible alert に集約した。
- Focused test で ORCA正本確認不能の警告が `通信詳細` details 内に閉じ込められていないことを固定した。

## Verification

- `cd web-client && npm test -- --run src/features/patients/__tests__/PatientsPage.test.tsx`: PASS.
- `cd web-client && npm run typecheck`: PASS.
- `cd web-client && npm run verify:web-guard`: PASS via `pretest` and `pretypecheck`.
- `bash server-modernized/tools/ci/check-doc-links.sh`: PASS.
- `bash server-modernized/tools/ci/check-config-contract.sh`: PASS.
- `git diff --check`: PASS.

## Residual Risk

- 今回は Patients の同期停止警告に限定した。Administration / Charts の debug-only meta disclosure は次回以降も重要警告が隠れていないか継続確認する。
