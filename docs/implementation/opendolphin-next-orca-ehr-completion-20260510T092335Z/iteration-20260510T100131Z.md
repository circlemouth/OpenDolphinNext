# Iteration 20260510T100131Z

## Scope

- Checklist item: Phase 1 local patient CRUD removal
- Selected item: `/api/local/patients/mutation` を廃止する
- Goal: remove the unused local patient mutation implementation itself, not only the production registration/admin wrapper surface.

## Changes

- Deleted the local patient mutation DTOs:
  - `api-contract/src/main/java/open/dolphin/rest/dto/orca/LocalPatientMutationRequest.java`
  - `api-contract/src/main/java/open/dolphin/rest/dto/orca/LocalPatientMutationResponse.java`
- Deleted the unregistered local patient mutation resource:
  - `server-modernized/src/main/java/open/dolphin/rest/orca/LocalPatientMutationResource.java`
- Deleted the direct unit test for the removed resource:
  - `server-modernized/src/test/java/open/dolphin/rest/orca/LocalPatientMutationResourceIdempotencyTest.java`
- Updated `docs/contracts/orca-route-taxonomy.md` to state that local patient CRUD DTO/resource/admin wrapper surfaces must not be reintroduced.
- Marked the checklist item `/api/local/patients/mutation` removal as complete.
- Hardened `web-client/scripts/verify-no-public-secrets.mjs` so the guard skips tracked files deleted in the working tree. This keeps guard verification usable before commit when a security remediation removes source files.

## Threats And Mitigations

- Threat: a removed but still compiled local CRUD resource can be accidentally re-registered later and allow patient create/update outside ORCA authority.
  Mitigation: removed the resource class and DTOs, then verified route inventory and server compilation.
- Threat: admin/internal wrapper UI or mocks can keep a misleading local patient mutation affordance alive.
  Mitigation: prior iteration removed wrapper capability, client caller, and MSW handler; this iteration removed remaining server-side implementation artifacts.
- Threat: security guards that fail on deleted tracked files can block verification and encourage skipping guard checks.
  Mitigation: updated the guard to skip non-existent tracked files while still scanning active source/config/docs.

## Verification

- `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=PublicRouteInventoryContractTest,AdminOrcaCapabilitiesResourceTest,PatientModV2OutpatientSupportTest,PatientModV2OutpatientResourceIdempotencyTest test`
  - Result: PASS, 16 tests.
- `cd web-client && npm run verify:web-guard`
  - Result: PASS.
- `cd web-client && npm test -- --run src/features/administration/orcaInternalWrapperApi.test.ts src/features/administration/__tests__/AdministrationPage.internalWrapper.test.tsx src/features/administration/__tests__/AdministrationPage.connection.test.tsx src/features/administration/__tests__/AdministrationPage.searchParams.test.tsx`
  - Result: PASS, 4 files / 16 tests.
- `cd web-client && npm run typecheck`
  - Result: PASS.
- `bash server-modernized/tools/ci/check-doc-links.sh`
  - Result: PASS.
- `git diff --check`
  - Result: PASS.
- `rg -n "LocalPatientMutation|/api/local/patients/mutation" api-contract server-modernized web-client docs/contracts docs/implementation/opendolphin-next-orca-ehr-completion-20260510T092335Z --glob '!docs/archive/**' --glob '!docs/reference/**'`
  - Result: only historical workstream notes and the completed checklist item remain; no active source, contract DTO, server resource, test, or web-client caller remains.

## Residual Risk

- Broader patient local-authority cleanup remains incomplete: `d_patient` still exists as a local table and must be redesigned into ORCA cache/snapshot semantics in later phases.
- Other local CRUD surfaces, especially diagnosis, still need the same removal/replacement treatment.
- Full milestone gates were not run in this 30-minute slice.

## Next Priority

Start Phase 1 local diagnosis CRUD removal by inventorying `/api/local/diagnoses`, `RegisteredDiagnosisModel` usage, ORCA disease adapters, and UI callers, then remove or fail-close the local disease-authority mutation path in the smallest safe slice.
