# Iteration 20260510T171528Z

## Checklist Item

- 17. 旧実装・危険経路の削除チェック: 診療録確定済みタイトル直接更新経路を封じる。

## Threat Model

- 確定済み診療録のタイトルだけが revision/event なしで直接変更され、content hash と監査上の真正性が崩れる。
- client が `/karte/document/{id}` へ直接 PUT しても、サーバー側が文書 status を authority として再確認すること。
- 監査ログ write path が利用できない場合は前回 guard により `updateTitle` 呼び出し前に停止すること。

## Changes

- `KarteDocumentWriteService.updateTitle` で対象 document を DB から取得し、`TMP` 以外の status は既存本文更新拒否と同じ `karte.document.finalized_update_denied` / HTTP 409 で拒否する。
- `updateTitle` の draft 成功・finalized 拒否 regression を追加した。
- `KarteDocumentWriteResource.putTitle` が audit write path unavailable 時に service 呼び出し前に 503 で停止する regression を追加した。
- checklist に確定済みタイトル直接更新の封鎖完了を追記した。

## Verification

- PASS: `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=KarteServiceBeanDocPkTest,KarteResourceDocumentContractTest test`
- PASS: `bash server-modernized/tools/ci/check-doc-links.sh`
- PASS: `bash server-modernized/tools/ci/check-config-contract.sh`
- PASS: `bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"`
- PASS: `cd web-client && npm run verify:web-guard`
