# Iteration 20260510T140728Z

## Selected Checklist Item

- 9.4 病名レスポンス処理: 警告、不一致、ORCA側のみ存在する病名を無視しない。
- 9.5 病名UI: 警告・不一致・ORCA側のみ病名を折りたたまず初期表示する。

## Scope

`diseasev3` mutation response の `warnings[]` と `unmatchInformation[]` を Web client の result model に取り込み、`DiagnosisEditPanel` で `ORCA病名送信の要確認` パネルとして表示する。成功・失敗バナーに潰さず、連携状態、ORCA結果、警告、不一致病名の sanitized summary を初期表示する。

## Threat Model

- ORCA warning / unmatch が成功扱いに見えて利用者確認が抜ける。
- ORCA側のみ存在する病名が折りたたまれ、レセプト影響のある不一致が見落とされる。
- raw ORCA body、内部URL、患者詳細、保険詳細が UI / tracked evidence に混入する。

## Controls

- `needsUserReview`、`ORCA_WARNING`、`ORCA_UNMATCHED`、`warnings[]`、`unmatchInformation[]` を review panel の表示条件にした。
- 表示対象は server が normalize 済みの固定フィールドに限定した。
- raw response は UI へ出さず、tracked iteration doc にも raw ORCA body / credential / patient detail を残していない。

## Verification

- `cd web-client && npm test -- --run src/features/charts/__tests__/DiagnosisEditPanel.test.tsx src/features/charts/diseaseApi.test.ts`
- `cd web-client && npm run typecheck`
- `cd web-client && npm run verify:web-guard`
- `bash server-modernized/tools/ci/check-doc-links.sh`
- `bash server-modernized/tools/ci/check-config-contract.sh`
- `bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"`
- `git diff --check`

## Notes

No raw credential, Cookie, Authorization, JSESSIONID, CSRF, raw ORCA body, patient name, address, phone number, insurance detail, HAR, trace, video, or screenshot evidence was added.
