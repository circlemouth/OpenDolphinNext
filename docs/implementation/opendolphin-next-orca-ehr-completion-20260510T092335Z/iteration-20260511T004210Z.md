# Worker C Iteration 20260511T004210Z

## Scope

- Worker: C
- Queue item: C-08 prescription authority integration watch
- Checklist: 8, 10.2 candidate row-sequence handoff; no checklist item newly closed
- Branch: `codex/orca-ehr-worker-c-prescription`
- Worktree: `/Users/Hayato/.codex/worktrees/opendolphinnext-orca-ehr-worker-c/OpenDolphin_WebClient`

## Assets / Trust Boundary / Attack Surface

- Assets: prescription RP/item sequence identity, `orca_medical_candidate` API JSON, Charts candidate parser.
- Trust boundary: client asks only by `chartRevisionId`; row identity is derived from saved prescription revision order on the server.
- Attack surface: candidate prepare API and later send/re-fetch/diff handoff consumers.

## Misuse Cases

- Send/re-fetch diff logic matches rows by display order only and reports a result against the wrong RP.
- Duplicate drug code/name rows become indistinguishable in candidate JSON.
- Client-supplied row identity is trusted instead of server-derived prescription revision order.
- Candidate UI or future send code cannot point unresolved issues back to stable RP/薬剤 positions.

## Implementation

- Added `rpSequence` to `ChartSupportMedicalModV2Request.MedicalInformation`.
- Added `itemSequence` to `ChartSupportMedicalModV2Request.Medication`.
- Populated both fields in `LocalOrcaMedicalCandidateResource` from saved prescription revision iteration order.
- Updated server and web focused tests plus Charts candidate API parser/types.
- Documented row-sequence handoff in prescription authority and UI current contracts.

## Verification

- PASS: `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=LocalOrcaMedicalCandidateResourceTest test`
- PASS: `cd web-client && npm test -- --run src/features/charts/__tests__/orcaMedicalCandidateApi.test.ts`
- PASS: `cd web-client && npm run typecheck`

## Docs Updated

- `docs/contracts/prescription-authority.md`
- `web-client/notes/ui-current-contract.md`
- `docs/implementation/opendolphin-next-orca-ehr-completion-20260510T092335Z/worker-board.md`

## Residual Risk / Next

- Live `medicalmodv2` send, post-send ORCA re-fetch, and diff reconciliation remain Worker D scope.
- C-09 should remain a narrow integration watch task for Worker C-owned prescription/candidate conflicts.
