# Iteration 20260510T192054Z

## Selected checklist item

- Section 17: ローカル患者作成/更新 API を削除する。

## Scope

- Added `web-client/scripts/verify-no-local-patient-mutation.mjs`.
- Wired `verify:no-local-patient-mutation` into `web-client` `verify:web-guard`.
- Marked the Section 17 local patient mutation removal item complete with the new regression guard evidence.

## Threat model

- Misuse case 1: a local patient create/update route is reintroduced and lets the browser treat OpenDolphinNext as the patient master.
- Misuse case 2: a legacy patient mutation resource or DTO is restored and bypasses the ORCA official patientmodv2 bridge.
- Misuse case 3: current docs or active source normalize `/api/local/patients/mutation(s)` as an allowed route, causing downstream implementation drift.

## Controls

- The guard scans active source and current contract/runbook docs only. Historical implementation evidence under `docs/implementation` remains available without failing the current contract.
- The guard fails closed on `/api/local/patients/mutation` and `/api/local/patients/mutations`.
- The guard also fails on `LocalPatientMutationResource`, `LocalPatientMutationRequest`, and `LocalPatientMutationResponse` in active roots.

## Verification

- `cd web-client && npm run verify:web-guard` passed.
- `cd web-client && npm run typecheck` passed.
- `bash server-modernized/tools/ci/check-doc-links.sh` passed.
- `bash server-modernized/tools/ci/check-config-contract.sh` passed.
- `bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"` passed.
- `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=PublicRouteInventoryContractTest,WebXmlEndpointExposureTest test` passed with 5 tests.
- Initial Maven attempt without `-Dsurefire.failIfNoSpecifiedTests=false` failed before server-modernized because sibling modules do not contain the named tests; the focused command above is the valid scoped form for this reactor.

## Evidence policy

- No raw credentials, cookies, authorization headers, raw ORCA bodies, patient names, addresses, phone numbers, insurance details, HAR, traces, videos, or screenshots were added.
