# Iteration 20260510T163352Z

## Checklist Item

- 16.2: ORCA接続、証明書期限、DB、監査ログ書き込みの health check を実装する。
- 16.4 follow-up: DB障害時読み取り専用モードの operational contract を、会計送信と再照合の fail-closed 実装へ接続する。

## Scope

- `/api/health/readiness` に sanitized `auditLog` check を追加する。
- authoritative audit chain head の write path lock が取れない場合、readiness を fail-closed にする。
- 会計送信と `tmedicalgetv2` 再照合は、監査ログ write path が利用できない場合に ORCA transport 前で 503 にする。

## Threat Model

- ORCA送信だけが成功し、監査ログ append が失敗して真正性証跡が欠落する。
- DB read-only / standby / restore 中に `ORCA_UNKNOWN` 再照合結果だけが UI に返り、永続監査なしの運用判断に使われる。
- readiness が database `UP` だけで成功し、authoritative audit chain head 欠落や write path degraded を検知できない。

## Changes

- `AuthoritativeAuditRepository`
  - `isWritePathAvailable()` を追加し、`audit_chain_head` の `SELECT ... FOR UPDATE` が可能かを rollback-only probe として確認する。
- `OperationsReadinessEvaluator`
  - `auditLog` check と `audit_log_write_unavailable` reason code を追加。
  - `auditLog` が `DOWN` の場合、overall readiness を `DOWN` にする。
- `LocalEncounterBillingWorkflowResource`
  - `close-and-send-to-billing` と `reconcile-temporary-medical` の入口で audit write path を確認し、利用不可なら sanitized 503 を返す。
- `docs/contracts/health-endpoints.md`
  - readiness contract に `auditLog` check を追加。
- `opendolphin-next-orca-ehr-implementation-checklist.md`
  - health check / audit write path fail-closed の完了サブ項目を追加。

## Verification

- Initial run failed as expected because upstream reactor modules had no matching tests without `-Dsurefire.failIfNoSpecifiedTests=false`.
- PASS: `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=OperationsHealthResourceTest,OrcaBillingCorrectionScenarioSupportTest test`
- PASS: `bash server-modernized/tools/ci/check-doc-links.sh`
- PASS: `bash server-modernized/tools/ci/check-config-contract.sh`
- PASS: `bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"`
- PASS: `cd web-client && npm run verify:web-guard`
- PASS: `git diff --check`

## Residual Risk

- 診療録保存、処方保存、添付操作など会計送信以外の mutation への read-only mode 強制は次工程。
- `auditLog` readiness は write path lock の確認であり、実イベント append / outbox publish の完全 rehearsal ではない。
