# Worker F Iteration 20260511T101225Z

- Worker: F
- Branch: `codex/orca-ehr-r2-worker-f-security-gates`
- Worktree: `/Users/Hayato/.codex/worktrees/opendolphinnext-orca-ehr-r2-worker-f/OpenDolphin_WebClient`
- HEAD at start: `7472d741f`
- Scope: checklist 13 / 18 / 19, Phase 5 reviewer packet sensitive evidence guard follow-up

## Startup Checks

- `pwd`: `/Users/Hayato/.codex/worktrees/opendolphinnext-orca-ehr-r2-worker-f/OpenDolphin_WebClient`
- `git rev-parse --show-toplevel`: `/Users/Hayato/.codex/worktrees/opendolphinnext-orca-ehr-r2-worker-f/OpenDolphin_WebClient`
- `git branch --show-current`: `codex/orca-ehr-r2-worker-f-security-gates`
- `git status --short`: clean

## Threat Model

- Assets: reviewer submission packet `closeout-packet/` reports, QA summaries, evidence JSON/log/MD, and copied docs.
- Trust boundary: local diagnostic browser artifacts are untrusted release evidence; reviewer packet may contain only sanitized extracted subsets.
- Attack surface: closeout summaries and final reports can point reviewers at local-only raw artifacts even if those artifacts are not copied.
- Misuse cases:
  - A fullflow summary includes `trace-*.zip` and gets treated as reviewer evidence.
  - A final report links `error-context.md` or screenshots that can contain DOM/request context.
  - A QA JSON includes `raw-response.json` / `raw-body.txt` and bypasses existing request XML / HAR checks.

## Implementation

- Extended `scripts/reviewer-submission-packet.mjs` forbidden closeout text patterns to reject `error-context.md`, trace zip, browser video, screenshots, and raw body/text/json artifact references.
- Added focused `tests/review-packet/reviewer-submission-packet.test.mjs` regression tests for summary-level trace archive references and report-level `error-context.md` / screenshot references.
- Updated reviewer packet and release validation runbooks.
- Recorded the checklist evidence note for the security evidence guard.
- Updated `ChartRevisionAuthorityMigrationTest` to use the current canonical Flyway migration filenames after the chart revision migrations moved to `V0323` / `V0324`, unblocking the focused schema test.

## Verification

- Passed: `node --test tests/review-packet/reviewer-submission-packet.test.mjs` (16 tests).
- Passed: `bash server-modernized/tools/ci/check-sensitive-evidence-redaction.sh --root "$(git rev-parse --show-toplevel)"`.
- Passed: `bash server-modernized/tools/ci/check-audit-append-only.sh --root "$(git rev-parse --show-toplevel)"`.
- Passed: `bash server-modernized/tools/ci/check-doc-links.sh`.
- Passed: `bash server-modernized/tools/ci/check-config-contract.sh`.
- Passed: `bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"`.
- Passed: `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=ChartRevisionAuthorityMigrationTest test`.
- Blocked: first `cd web-client && npm run ci` could not start because `node_modules` was absent and `tsc` was unavailable. After `npm install`, `npm run ci` reached Vitest and failed on two pre-existing/non-F-owner blockers:
  - `scripts/__tests__/orcaBillingReportLiveProfileEvidence.test.ts`: the summary JSON includes the literal forbidden field label `rawMedicalUid` inside `forbiddenEvidenceFields`, tripping the forbidden `Medical_Uid` evidence regex.
  - `src/features/charts/__tests__/chartsActionBar.orca-send.test.tsx`: `getByText(/ORCA送信/)` is now ambiguous because both the note and completion text match.
- Blocked: `mvn -f pom.server-modernized.xml -pl server-modernized -am -Pstatic-analysis verify` passed the test/failsafe phase (`server-modernized` test summary: 1155 run, 0 failures, 0 errors, 3 skipped; failsafe 9 run, 0 failures) but failed at SpotBugs with 35 medium findings in ORCA/chart DTO/cache classes.
- Note: `npm install` reported `9 vulnerabilities (4 low, 3 moderate, 2 high)` in dependency audit metadata. No dependency changes were committed in this heartbeat.

## Residual Blockers

- Web full CI requires Worker D/F evidence test wording cleanup for `rawMedicalUid` or a narrower assertion that does not embed forbidden PHI labels in sanitized evidence, plus Worker E/C chart action bar query disambiguation.
- Server static-analysis verify requires either fixing or explicitly triaging the 35 current SpotBugs medium findings before release gate can be green.
