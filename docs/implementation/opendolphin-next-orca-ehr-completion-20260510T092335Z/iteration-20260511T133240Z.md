# Worker A Iteration 20260511T133240Z

## Scope

- Worker: A
- Branch: `codex/orca-ehr-r2-worker-a-orca-boundary-live`
- Queue: R2-A05 live Trial patient/acceptance/insurance evidence watch
- Checklist: 18 live Trial patient/acceptance/insurance read-only / approved mutation readiness evidence

## Threat Model

- Assets: exact selected-candidate preflight summary, Phase 3 handoff eligibility, sanitized `mutationPolicy`, patient / appointment / insurance readiness evidence.
- Trust boundaries: browser UI events and request observations are untrusted evidence inputs; Phase 3 mutation requires exact preflight plus approved command guards.
- Attack surface: unexpected UI mutation during read-only preflight, incomplete request capture for mutation route drift, duplicate browser route/request events inflating or leaking evidence.

## Misuse Cases

1. The Reception UI unexpectedly calls `acceptance-operation`, `medical-mod-v2`, `disease-mod-v3`, local billing send, or temporary medical reconcile during read-only preflight.
2. A mutation request contains patient ID, query token, insurance hint, or request body labels and is copied into tracked preflight evidence.
3. A blocked mutation request is observed twice through route and request events, producing inconsistent blocker counts or noisy evidence.

## Implementation

- Added the shared `isReadonlyBlockedMutationUrl` blocker to `qa-weborca-readonly-preflight.mjs` browser routing.
- Aborts shared blocked mutation routes with `blockedbyclient` before the UI can complete them.
- Records only sanitized route template, method, and fixed reason in `requestRecords`.
- Dedupes blocked mutation records by sanitized route template / method / reason.
- Added a focused script guard test so exact preflight keeps the shared browser mutation blocker installed.

## Verification

- `cd web-client && npm test -- --run scripts/__tests__/orcaTrialPreflight.test.ts`
- `cd web-client && npm run verify:web-guard`
- `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dtest=PublicRouteInventoryContractTest -Dsurefire.failIfNoSpecifiedTests=false test`
- `bash server-modernized/tools/ci/check-doc-links.sh`
- `bash server-modernized/tools/ci/check-config-contract.sh`
- `bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"`
- `bash server-modernized/tools/ci/check-sensitive-evidence-redaction.sh --root "$(git rev-parse --show-toplevel)"`
- `git diff --check`

## Result

- Exact selected-candidate read-only preflight now aborts the same unexpected mutation routes as candidate discovery.
- Blocked mutation evidence remains sanitized and deduped.
- Actual live Trial execution remains unperformed in this heartbeat; checklist live connection items stay open.
