# Iteration 20260510T123130Z

## Selected checklist item
- 3.4 / 9.2 / 17: `diseasev2`、旧 CLAIM 病名送信、ORCA 患者病名 DB 直接参照を病名正本経路から撤去する。

## Work completed
- `OrcaDiseaseQuerySupport` から未使用の `tbl_ptbyomei` 直接参照 helper を削除し、ORCA 病名正本の取得経路を `diseasegetv2` mirror / `diseasev3` mutation contract に寄せた。
- `server-modernized/tools/ci/check-no-legacy-disease-authority.sh` を追加し、active modernized roots に `diseasev2`、旧 CLAIM 病名送信、ORCA 患者病名 DB 直接参照が再混入した場合に失敗するようにした。
- `DiagnosisSendWrapper` の古い CLAIM 病名送信説明を、現状の互換ローカル更新 wrapper としての説明に修正した。
- checklist、Disease / ORCA contract、release validation runbook に guard と完了状態を反映した。

## Threat model
- Misuse case 1: 開発者が ORCA `diseasegetv2` mirror ではなく ORCA 患者病名 DB を直接読む helper を再導入する。
  - Mitigation: active source / tests / runner roots を guard で走査し、`tbl_ptbyomei` / `ptbyomei` を検出したら fail closed。
- Misuse case 2: 旧 CLAIM 病名送信の transport を modernized scope に戻し、`diseasev3` の警告・不一致・再取得 semantics を迂回する。
  - Mitigation: CLAIM と病名 / diagnosis / disease が同一行に現れる transport marker を guard で検出する。
- Misuse case 3: `diseasev2` の旧 API 名または alias を新規実装・fixture に混入させる。
  - Mitigation: active roots の `diseasev2` / `diseasemodv2` literal を guard で検出する。

## Verification
- `bash server-modernized/tools/ci/check-no-legacy-disease-authority.sh --root "$(git rev-parse --show-toplevel)"` - passed.

## Residual risk
- `DiagnosisSendWrapper` / `KarteDiagnosisService` による local diagnosis CRUD は別 checklist 項目として残る。今回の guard は diseasev2 / CLAIM 病名送信 / ORCA 患者病名 DB 直接参照の再混入防止に限定している。

## Next
- `POST /api/local/diagnoses` と `KarteDiagnosisService` 由来の local diagnosis create / update / delete 公開面を fail closed に寄せ、ORCA `diseasev3` operation に一本化する。
