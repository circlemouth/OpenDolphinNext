# Iteration 20260510T192926Z

## Selected checklist item

- Section 17: 診療録確定済みタイトル直接更新経路、確定済み処方 payload 直接上書き経路を削除する。

## Scope

- Added `server-modernized/tools/ci/check-finalized-write-guards.sh`.
- Added the guard to `docs/runbooks/release-validation.md`.
- Marked the Section 17 finalized chart title / prescription payload overwrite item complete with guard evidence.

## Threat model

- Misuse case 1: finalized chart title update is accidentally relaxed and mutates a finalized document in place.
- Misuse case 2: prescription save accepts a closed/billing-waiting encounter and overwrites local draft payload after chart/billing closure.
- Misuse case 3: DO import merges and persists prescription payload before server-side encounter projection denies a closed encounter.

## Controls

- The guard requires the finalized chart title denial implementation and regression test to remain present.
- The guard requires prescription save and DO import to call server-side encounter mutability checks before persistence.
- The guard requires regression tests proving closed encounter denial returns conflict and does not save payload.

## Verification

- `bash server-modernized/tools/ci/check-finalized-write-guards.sh --root "$(git rev-parse --show-toplevel)"` passed.
- `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=KarteServiceBeanDocPkTest,LocalPrescriptionOrderResourceTest test` passed with 21 tests.
- `bash server-modernized/tools/ci/check-doc-links.sh` passed.
- `bash server-modernized/tools/ci/check-config-contract.sh` passed.
- `bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"` passed.
- `cd web-client && npm run verify:web-guard` passed.
- Initial guard run caught an overly broad DO import ordering assertion; the guard was corrected to verify the public DO import call order and the validation method's pre-return mutability check separately.

## Evidence policy

- No raw credentials, cookies, authorization headers, raw ORCA bodies, patient names, addresses, phone numbers, insurance details, HAR, traces, videos, or screenshots were added.
