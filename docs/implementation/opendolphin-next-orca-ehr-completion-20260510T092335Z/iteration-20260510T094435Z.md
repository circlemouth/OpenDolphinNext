# Iteration Report

- RUN_ID: `20260510T094435Z`
- Automation: `opendolphinnext-orca-ehr`
- Checklist item: Phase 1 local patient CRUD removal
- Branch: `master`

## Work Completed

- Removed `LocalPatientMutationResource` from `OpenDolphinRestApplication` production route registration.
- Removed `/api/local/patients/mutation` from `PublicRouteInventoryContractTest` expected local route inventory.
- Removed the local patient mutation admin capability from `AdminOrcaCapabilitiesResource`.
- Removed the patient mutation option and runtime caller from the Administration internal wrapper UI.
- Removed the MSW handler for `/api/local/patients/mutation`.
- Updated `docs/contracts/orca-route-taxonomy.md` so local patient mutation is no longer a current public taxonomy route.
- Corrected this workstream checklist API examples to obey current taxonomy: public ORCA routes are under `/api/orca/official/*` or `/api/orca/master/*`; local prepare/candidate routes use `/api/local/*`.

## Threats And Mitigations

- Threat: a browser or admin wrapper can create/update patient rows through `/api/local/patients/mutation`, bypassing ORCA `patientmodv2` authority.
  - Mitigation: removed the resource from production JAX-RS registration and removed web admin runtime callers.
- Threat: test/MSW fixtures keep the deleted route looking valid.
  - Mitigation: removed the MSW route handler and updated route inventory expectations.
- Threat: implementation docs introduce new `/api/orca/*` paths outside official/master taxonomy.
  - Mitigation: rewrote checklist planned paths to current taxonomy-compliant paths and reran the route guard.

## Verification

- `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=PublicRouteInventoryContractTest,AdminOrcaCapabilitiesResourceTest test` -> PASS
- `cd web-client && npm run verify:web-guard` -> PASS
- `cd web-client && npm test -- --run src/features/administration/orcaInternalWrapperApi.test.ts src/features/administration/__tests__/AdministrationPage.internalWrapper.test.tsx src/features/administration/__tests__/AdministrationPage.connection.test.tsx src/features/administration/__tests__/AdministrationPage.searchParams.test.tsx` -> PASS
- `cd web-client && npm run typecheck` -> PASS
- `bash server-modernized/tools/ci/check-doc-links.sh` -> PASS
- `git diff --check` -> PASS

## Residual Risk

- `LocalPatientMutationResource`, `LocalPatientMutationRequest`, `LocalPatientMutationResponse`, and their direct unit test still exist but are no longer registered in production route inventory. The next slice should delete or quarantine the unused class/DTO/test and confirm no compile/runtime dependency remains.
- Local diagnosis CRUD remains active and is the next larger Phase 1 boundary issue.

## Next Priority

Delete or quarantine the unused local patient mutation resource/DTO/test, then rerun server compile and inventory tests. After that, begin the local diagnosis CRUD replacement plan.
