# Worker E Iteration 20260511T030213Z

## Scope
- Queue: E-04 follow-on.
- Checklist: 11 disabled dependency reason display; 20 Phase 4 Medical Safety UI / DADS.
- Surface: `PastHubPanel` SOAP Do-copy entry points.

## Threat Model
- Asset: patient-scoped SOAP draft and historical chart reference workflow.
- Trust boundary: past-chart UI selection is client-side convenience; draft application remains handled by Charts page state.
- Attack surface: disabled Do-copy controls when no transferable SOAP source exists or a section has no note.

## Misuse Cases
- A user sees `この日のSOAPをまとめてDo` disabled and cannot tell whether there is no source SOAP or the workflow is unavailable.
- A user sees section-level `Do転記` disabled without knowing the section has no note.
- Assistive technology exposes disabled Do-copy controls without a related reason.

## Implementation
- Kept native disabled for missing SOAP source / missing section note states.
- Added nearby `past-hub-do-copy-*` reason text for batch and section Do-copy buttons.
- Connected disabled Do-copy buttons to the reasons with `aria-describedby`.

## Verification
- `cd web-client && npm test -- --run src/features/charts/__tests__/PastHubPanel.test.tsx`
- `cd web-client && npm run verify:web-guard`
- `cd web-client && npm run typecheck`
- `git diff --check`

## Docs
- Updated `web-client/notes/ui-current-contract.md`.
- Updated `worker-board.md`.
- Updated `opendolphin-next-orca-ehr-implementation-checklist.md` only for the verified item.

## Residual Risk
- A broader disabled-surface audit remains for non-OrderDock Medical Safety UI controls.

## Next
- Continue E-04 remaining Medical Safety UI disabled surface audit, then move to checklist 17 remaining items if no E-04 issues remain.
