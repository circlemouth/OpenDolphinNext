# Worker F Iteration 20260510T215924Z

## Scope

- Worker: F
- Branch: `codex/orca-ehr-worker-f-audit-security-gates`
- Queue item: F-04 real ORCA connection trial checklist execution harness with sanitized evidence policy
- Checklist scope: 18, 19

## Starting State

- `pwd`: `/Users/Hayato/.codex/worktrees/opendolphinnext-orca-ehr-worker-f/OpenDolphin_WebClient`
- `git rev-parse --show-toplevel`: `/Users/Hayato/.codex/worktrees/opendolphinnext-orca-ehr-worker-f/OpenDolphin_WebClient`
- branch: `codex/orca-ehr-worker-f-audit-security-gates`
- `git status --short`: clean at iteration start

## Assets / Trust Boundary / Attack Surface

- Assets: live WebORCA Trial credentials, runtime ORCA settings, Trial candidate identifiers, QA summaries, reviewer evidence, release validation command log.
- Trust boundary: candidate discovery is a proposal only; exact read-only preflight and matching hash/input identity are required before mutation. HTTP 200 and generic zero-like `apiResult` are not mutation success.
- Attack surface: live QA script ordering, environment logging, browser artifacts, raw ORCA body/XML, raw network files, screenshots, HAR/trace/video, patient detail, insurance detail, credential-bearing URLs.

## Misuse Cases

1. Operator records ORCA Basic credential, URL with credential, cookie, CSRF token, raw ORCA body/XML, or certificate material in tracked evidence.
2. Candidate discovery or local patient search output is treated as Phase 3 mutation authorization without exact read-only preflight.
3. HTTP 200 or all-zero generic parser result is recorded as mutation success without endpoint-specific completion evidence.

## Implementation

- Added `ops/tests/orca/live-trial-checklist.sh` dry-run harness.
- Added `server-modernized/tools/ci/check-live-orca-trial-harness.sh`.
- Updated release validation and ORCA certification docs to invoke the dry-run harness before live Trial execution.
- Added focused guard tests for the live ORCA Trial harness policy.
- Updated Worker board and checklist with harness evidence while keeping actual live ORCA pass items open.

## Verification

```bash
ops/tests/orca/live-trial-checklist.sh --dry-run --run-id 20260510T215924Z
bash server-modernized/tools/ci/check-live-orca-trial-harness.sh --root "$(git rev-parse --show-toplevel)"
```

Additional completed checks before commit:

```bash
bash server-modernized/tools/ci/check-doc-links.sh
bash server-modernized/tools/ci/check-config-contract.sh
bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"
bash server-modernized/tools/ci/check-audit-append-only.sh --root "$(git rev-parse --show-toplevel)"
bash server-modernized/tools/ci/check-backup-restore-runbook.sh --root "$(git rev-parse --show-toplevel)"
bash server-modernized/tools/ci/check-sensitive-evidence-redaction.sh --root "$(git rev-parse --show-toplevel)"
mvn -f pom.server-modernized.xml -pl server-modernized -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=RepoGuardScriptsTest test
```

## Residual Risk

- This iteration added the execution harness and policy guard. It did not run live ORCA Trial mutation, fullflow, or Phase 4 medicalmodv2.
- Section 18 actual live ORCA checklist items remain open until live sanitized evidence exists.
