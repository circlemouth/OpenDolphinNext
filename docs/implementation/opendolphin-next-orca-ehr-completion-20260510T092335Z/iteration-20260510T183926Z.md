# Iteration 20260510T183926Z

## Checklist Item

- Section 17: ORCA送信成功を診療録確定と同義に扱う UI 文言、ORCA送信失敗時に登録済み/反映済みと表示する文言、重要警告を初期非表示にする UI を削除または変更する。

## Threat Model

- Misuse case 1: Reception の `送信済` transmission signal を、収納確認済みまたは `会計済み` workflow と誤認する。
- Misuse case 2: 会計待ちまたは再計待の患者カードで `送信済` だけが強調され、管理者確認や収納確認が不要に見える。
- Misuse case 3: row-local key に紐づく claim send cache の signal が、請求 workflow の authority と混同される。

## Changes

- Reception の billing projection で `送信済` かつ workflow が `会計済み` ではない場合、`送信済は会計済みではありません。収納確認まで会計待ちです。` を表示するようにした。
- カード表示と展開詳細の両方で transmission signal と billing workflow の境界を visible text で補強した。
- focused tests で `送信済` が `会計済み` として表示されないこと、再計待でも transmission note が見えることを固定した。

## Verification

- `cd web-client && npm test -- --run src/features/reception/__tests__/ReceptionPage.test.tsx`: PASS.
- `cd web-client && npm run typecheck`: PASS.
- `cd web-client && npm run verify:web-guard`: PASS via `pretest` and `pretypecheck`.
- `bash server-modernized/tools/ci/check-doc-links.sh`: PASS.
- `bash server-modernized/tools/ci/check-config-contract.sh`: PASS.
- `git diff --check`: PASS.

## Residual Risk

- 今回は Reception の billing projection 表示に限定した。Charts ActionBar / OrcaSummary の分離テストは既存化済みだが、Section 17 の残り文言は次回も継続して棚卸しする。
