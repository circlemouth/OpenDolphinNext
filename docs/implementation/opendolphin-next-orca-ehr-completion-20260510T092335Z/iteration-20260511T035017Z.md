# Worker B Iteration 20260511T035017Z

## Queue

- Worker: B
- Branch: `codex/orca-ehr-worker-b-chart-revision`
- Selected item: B-08i
- Checklist scope: 14 chart revision JSON export current revision content hash metadata
- Full B-08 remains pending until Worker A/C/D snapshot contracts are integrated.

## Assets / Trust Boundary / Attack Surface

- Assets: `chart_document.current_revision_id`, current revision content hash, chart revision JSON export hash material.
- Trust boundary: API callers do not provide current revision content hash; it is derived from server-side revision rows.
- Attack surface: current revision hash confusion, pointer/content tampering, omitted revision rows, unsafe summary payloads.

## Misuse Cases

- A consumer verifies a stale revision hash because the export envelope lacks current hash metadata.
- A corrupted current pointer changes without a matching current content hash in the export envelope.
- A revision row is omitted while `currentRevisionId` remains present in the export envelope.

## Implementation

- Added `currentRevisionContentHash` to `ChartRevisionExportResponse`.
- `ChartRevisionExportService` derives current revision number/status/content hash from the same exported current revision row.
- Added `currentRevisionContentHash` to canonical `exportHash` material.
- Existing fail-closed guard still rejects a `currentRevisionId` that is absent from the revision list.

## Verification

- `mvn -f pom.server-modernized.xml -pl server-modernized -am -Dtest=ChartRevisionExportServiceTest,PublicRouteInventoryContractTest -Dsurefire.failIfNoSpecifiedTests=false test`
- `bash server-modernized/tools/ci/check-doc-links.sh`
- `bash server-modernized/tools/ci/check-config-contract.sh`
- `bash server-modernized/tools/ci/check-no-direct-runtime-lookup.sh --root "$(git rev-parse --show-toplevel)"`
- `git diff --check`

## Docs

- Updated `docs/contracts/document-integrity.md`.
- Updated `opendolphin-next-orca-ehr-implementation-checklist.md`.
- Updated `worker-board.md`.

## Residual Risk

- Full patient/acceptance/insurance/disease/prescription/ORCA operation snapshot references still require Worker A/C/D contract integration.
