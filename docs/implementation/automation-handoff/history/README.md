# Handoff History

Archive completed or superseded `NEXT_WORKER_PROMPT.md` files here.

Suggested filename:

```text
YYYYMMDD-<source-work-order>-<blocker-id>-<status>.md
```

Do not move the active prompt here until `HANDOFF_STATE.json` and the final report have been updated.
